# GENESIS II — CONTINUOUS NQ ALPHA HUNT

Opened 2026-08-28 by owner directive ("PROJECT GENESIS II"). GENESIS I's STATE B is accepted
**only at its exact tested scopes** — it is a routing signal, not an endpoint. Two engines run in
parallel and neither waits for the other:

- **ENGINE A — WORLD ALPHA DISCOVERY**: continuously scans papers, practitioners, videos, forums,
  commercial indicators, and public code; converts claims into observable-mapped mechanism cards;
  formally tests 3–6 frozen primaries per wave. Novelty is legitimate in THREE forms:
  **RAW-INFO / REPRESENTATION / POLICY** — "no new raw data" is not a rejection reason.
- **ENGINE B — FORWARD EVIDENCE FACTORY**: shadow (start 2026-09-01 18:00 ET, owner action),
  FOLLOW_MORNING virgin read, LIQREV01/HTFDIR01 readings (2026-11-01), parked-object revival
  conditions. Frozen survivors route here; nothing backfills.

## Standing rules (deltas vs GENESIS I; everything else inherits)

1. **Ledger continuity**: formal trials continue in the ONE hash-chained ledger
   `research/genesis/SEARCH_LEDGER.jsonl` (`research_sdk/genesis_ledger.py`). A second ledger
   would fragment the multiplicity account. (Deviation from the directive's literal
   `genesis2/SEARCH_LEDGER.jsonl` path, recorded here; the directive's intent — no invisible
   trials — is what binds.)
2. **Queue replenishment**: if `ACTIVE_ALPHA_QUEUE.md` drops below 10 DISCOVERED or below 3
   MED/HIGH-prior ideas, the next world-scan wave launches automatically — "no current candidate"
   never silently becomes "stop researching".
3. **Closure language**: every closure names observable set, representation, horizon, timing,
   target, execution model, sample, evidence class. "X is dead" without a scope is a doc bug.
4. Discovery is broad and cheap; **formal testing stays disciplined**: one frozen primary per
   mechanism, spec committed before results, nulls with teeth, MDE printed, FAIL recorded as FAIL.
5. **Clean-room lane is open**: lawful behavioral reconstruction of commercial indicators from
   public material — mechanism, never source code. No licensing bypass, no piracy.
6. Anti-P1 supervised mining stays barred; portfolio complementarity is measured AFTER standalone
   survival. Protected pools spend only behind frozen candidates with power calcs, via the owner
   gate. **The agent never enables anything live** — but note a live real-money book exists on `2047681` since 2026-09-01 (`research/operational/CURRENT_LIVE_TRUTH.md`). $0 without the owner spend gate.

## Files

`WORLD_ALPHA_ATLAS.md` (mechanism cards) · `ACTIVE_ALPHA_QUEUE.md` (live queue + conversion
funnel) · `STRATEGY_FAMILY_TREE.md` · `SOURCE_GRAPH.md` · `FAILURE_MEMORY.md` (exact closed
scopes — the anti-rescue gate) · `MASTER_SCOREBOARD.md` · `RESUME.md` (written on interruption).
Runs: `runs/G2_*`.
