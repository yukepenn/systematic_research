========================================================================
OWNER AMENDMENT 6 — GLOBAL PARALLEL RESEARCH / NO PREMATURE CLOSURE
========================================================================

Continue autonomously.

The W4 alpha-throughput work is approved and constitutes real progress.

However, the DR-E global review contains useful PRIORS but overreaches in
turning those priors into universal closure rules.

Apply the following corrections immediately.

1. WITHDRAW "ZONE F FORMALLY CLOSED"

Replace with:

    FAST_STRUCTURAL prior materially downgraded.
    No qualified strategy exists yet.
    The lane remains OPEN for a bounded final high-EVI frontier.

Do NOT use a universal 30-minute minimum holding period.

The current local evidence rejects the strategies tested.
It does not mathematically reject every 5–120 second conditional strategy.

2. EXTERNAL HFT LATENCY-RACE EVIDENCE IS NOT A BOUND ON ALL FAST ALPHA

Latency-arbitrage literature concerning microsecond stale-quote races
strongly lowers the prior for:

    next-tick harvesting
    queue racing
    pure latency arbitrage

It does NOT establish that every 10–120 second structural NQ state has
<= 0.5 tick economic information.

Label this evidence EXTERNAL PRIOR, not local impossibility proof.

3. RETAIL DAY-TRADER FAILURE STATISTICS ARE PRIORS

Do not use statistics from discretionary retail traders in other futures
markets as a formal rejection of a systematic NQ hypothesis.

They justify skepticism, not closure.

4. WITHDRAW THE UNIVERSAL "30–60 MIN FRICTION FLOOR"

The horizon/capture-rate calculation is a useful heuristic.

It assumes capture efficiency that may not apply to selective structural
states.

Do not transfer Solar's approximate capture efficiency to all strategies.

Every family earns or fails on its own after-cost conditional expectancy.

5. DO NOT CALL KELLY SIZING A "CERTAIN WIN"

Sizing does not create alpha.

Study robust fractional-Kelly sizing under:

    estimation uncertainty
    right-tail concentration
    drawdown constraints
    future edge degradation.

Report a sizing frontier.

Do not present a single Kelly fraction as truth.

6. NON-OVERLAPPING HOLDING TIMES DO NOT IMPLY ORTHOGONAL PNL

The overnight family is promising precisely because it is mechanically
different from Solar.

But it must earn low correlation empirically.

No strategy is "orthogonal by construction" merely because its clock does
not overlap Solar.

7. RUN THREE PROGRAMS IN PARALLEL

PROGRAM A — FAMILY-A EXPLOITATION

Bounded work only:

    robust E10 sizing
    executable cost improvements
    timed-exit execution tests

Do not reopen Solar parameter optimization.

PROGRAM B — INDEPENDENT ENGINE DISCOVERY

This is the largest strategic objective.

Research mechanisms including:

    overnight / inventory
    failed persistence
    value / auction reacceptance
    opening structures
    event-state families
    cross-asset engines
    other economically distinct hypotheses generated by deep research.

Every new family:
    standalone after-cost evidence first
    then actual correlation with R5-E10.

PROGRAM C — BOUNDED FAST-SCALP FRONTIER

Complete the currently untested high-EVI fast hypotheses before closure:

    W5-1 deep-pullback / CLEAN entry
    fast FSS-2 breakout-acceptance
    FSS-3 failed-opposite-probe continuation
    FSS-6/7 compression / velocity-efficiency
    FSS-10 ES confirmation / non-confirmation

Do not resurrect already killed definitions.

Do not retune W4 losers.

8. ADD ONE PREDICTABILITY-CEILING TEST BEFORE FAST-SCALP CLOSURE

Use the existing causal discovery features to answer:

    IS THERE ENOUGH CONDITIONAL INFORMATION AT ALL?

Primary labels should include economically meaningful outcomes such as:

    P(+24 before -8)
    P(+32 before -10)
    CLEAN_MOVE targets.

Use day-aware chronological validation.

Start with:

    regularized logistic regression
    GAM
    shallow gradient boosting.

Purpose:

    measure achievable conditional probability lift,
    NOT optimize backtest PnL.

All preprocessing and feature/model selection remain inside training folds.

Report:

    calibration
    high-confidence bucket lift
    sample size
    C1 break-even gap
    C2 break-even gap
    stability by day/month
    feature dependence.

If a causal model cannot produce enough fresh conditional lift even
before translating to a strategy, that is strong evidence the information
set is insufficient.

If it CAN:
    freeze the model/rule,
    convert it to a sequential trade simulation,
    then require after-cost confirmation.

9. FAST-SCALP CLOSURE RULE

Zone F may be closed only after:

    the five remaining high-EVI mechanism families above are tested;
    the predictability-ceiling test is completed;
    no candidate survives C1 on a coherent plateau;
    no high-confidence conditional state closes the economic probability gap;
    independent red team agrees no major untested causal information family remains.

Then report:

    NO QUALIFIED FAST NQ SCALPING EDGE FOUND
    IN THE TESTED RESEARCH UNIVERSE.

Do NOT report:

    fast NQ scalp is mathematically impossible.

10. ALPHA THROUGHPUT REMAINS GOVERNING

Do not return to process theater.

Every wave must produce:
    trade readout,
    kill,
    promotion,
    or material independent information.

Run PROGRAM A, B, and C concurrently where compute permits.

The highest-value long-run outcome remains:

    multiple independent after-cost return engines
    + robust portfolio combination
    + disciplined sizing,

not merely optimizing the existing Solar engine.

Do not ask the owner for routine confirmation.

Push until the actual frontier, not an external prior, tells us to stop.
========================================================================
END AMENDMENT 6
========================================================================
