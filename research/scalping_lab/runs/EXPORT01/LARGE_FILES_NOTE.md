probe_ticks.csv files in these run dirs are ~141MB raw NT8 tick exports - over
GitHub's 100MB per-file limit, so they are gitignored and live ON DISK ONLY
(OneDrive-synced). Do not delete. The derived committed substrates are
research/scalping_lab/substrate/grid1s/ and sechilo/.
