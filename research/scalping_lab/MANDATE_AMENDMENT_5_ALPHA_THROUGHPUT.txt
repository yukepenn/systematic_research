OWNER EXECUTION PRIORITY — ALPHA THROUGHPUT MODE
(received 2026-08-08, archived verbatim)

Continue autonomously.

The research constitution, data substrate, cost model, BBO audit,
sample roles, and anti-hallucination framework are now sufficiently mature.

From this point forward, DO NOT let governance work become the research product.

Freeze only what is necessary to prevent hindsight selection.
A new hypothesis spec should normally be short and executable.
Then run it.

Unless a concrete anomaly threatens validity, do not spend new waves on:
- more Z1 analysis;
- more generic BBO audits;
- more substrate redesign;
- more taxonomy;
- more contamination framework;
- more generic opportunity maps.

Those are now infrastructure, not the mission.

The mission is ALPHA PRODUCTION.

Every research wave must produce at least one of:

A. a real sequential trade-rule readout;
B. a definitive family rejection;
C. a candidate promoted to fresh internal confirmation;
D. an independent feature that materially improves a surviving candidate.

A wave producing only reports/specs/audits is NO ALPHA PROGRESS.

Run independent high-EVI families IN PARALLEL where possible.

Immediate priority:

1. FSS-1 — impulse → shallow pullback → rebreak
2. FSS-2 — breakout → acceptance → continuation
3. FSS-3 — failed opposite probe → continuation
4. FSS-5 — meaningful level sweep → reclaim
5. frozen S2a
6. H-B5 spike continuation vs reversal
7. FSS-10 ES confirmation / non-confirmation
8. role-B/C microstructure timing only after structural candidates exist

Do not wait for one family to finish before constructing another
independent family when compute allows parallel execution.

Add a CLEAN_MOVE target family:

examples:
    MFE >= 16/20/24/32 ticks
    while pre-target MAE <= economically small bounds

and explicitly study path ordering:

    target-before-stop
    time-to-target
    time-to-adverse
    pre-target drawdown
    time-underwater.

We care about identifying moves that are not merely large,
but TRADEABLE FROM THE ENTRY POINT.

The key problem revealed by the census is not move availability.
Large 30–60 second NQ moves are abundant.

The problem is:

    direction
    path quality
    entry selectivity
    retention.

Search for conditional lift in COMBINATIONS OF REAL MARKET STRUCTURE,
not single ubiquitous triggers.

Prefer architectures such as:

    structural opportunity
    + failure/acceptance evidence
    + independent confirmation
    + precise entry timing.

Do not require one primitive to generate the entire edge.

Do not brute-force unrestricted feature combinations.

If a structural base signal shows some genuine lift,
test a small number of independent additions such as:
    ES confirmation
    spread regime
    flow confirmation
    location
one at a time and then in minimal combinations.

The economically important benchmark is now the larger fast-scalp geometry:
roughly 10–32 tick opportunities over seconds-to-minutes,
not next-tick prediction.

Use C1 as primary promotion truth and C2 as stress.

Do not consume INTERNAL CONFIRMATION merely because a discovery rule is
slightly positive.

But do not demand perfection in discovery either.

A candidate earns confirmation when it has:
    positive after-C1 expectancy,
    coherent neighboring definitions,
    credible mechanism,
    nontrivial conditional lift,
    acceptable concentration,
    enough independent days.

Once one candidate earns that bar:
FREEZE IT AND IMMEDIATELY RUN FRESH INTERNAL CONFIRMATION.

Do not keep optimizing discovery after a candidate qualifies.

Likewise, once a family has a coherent negative plateau:
KILL IT AND MOVE ON.

Begin producing actual fixed-1-NQ StrategyV01 implementations as soon as
a rule family has credible positive trade economics.
Do not wait until the entire campaign is over.

Keep a visible ALPHA THROUGHPUT scoreboard:
    families tested
    families killed
    candidates alive
    candidates in confirmation
    StrategyV01 built
    best C1 net ticks/trade
    best win rate
    best Sharpe/DD frontier.

Continue without asking the owner.

The goal is not more research process.

The goal is:
    FIND THE BEST REAL SYSTEMATIC NQ SCALP WE CAN FIND,
    OR PROVE THAT THE SERIOUS SEARCH SPACE FAILS.

Push.
