# Scalping Lab experiment registry — one entry per preregistered experiment
# schema: {id, date, spec, hypothesis_ids, tier, status, verdict, artifacts}
experiments: []

# NOTE (appended 2026-08-18, consolidation pass): this file was never used during the
# campaign (empty since creation). The operative per-experiment records are specs/ (15
# frozen wave specs, committed before readouts), tested_configs.csv (S1-S35) and
# CAMPAIGN_STATE.md. See registry/README.md. Do not interpret the empty list as
# "no experiments were preregistered".
