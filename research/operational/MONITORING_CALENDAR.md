# MONITORING_CALENDAR — every scheduled or gated future read, all programs (2026-08-28)

Single consolidated schedule. Each row cites its governing protocol; this file adds no new
authority — regenerate/extend it whenever a protocol changes. Dates are "due on/after".

## Dated

| Due | Read | Object | Protocol / rule | Notes |
|---|---|---|---|---|
| **2026-09-01 18:00 ET** | **`SHADOW_START` — begin PROSPECTIVE SHADOW accumulation** (logging only; **no orders, no Sim101, no live account**) | incumbent roster: `P1/PCT` (`ee4c765b…`), `XM_CONFLICT_v2` (`2ec00dd4…`). ⚠️ **`P1/ABS` (`e8bb9caf…`) REMOVED from the roster 2026-08-30** — `runs/G2_ABS_VS_PCT_20260830/` settled its direction question for free on burned data (the recorded 176/213 result was a cost artifact) and showed the shadow is **~5× under-powered** on magnitude (power 13.3% @52wk). Quarantined out of the NT8 Strategies folder the same day. | `PROSPECTIVE_SHADOW.md`; ledger `research_sdk/shadow_ledger.py` | ⚠️ **This is a WRITE, not a READ — it adjudicates nothing.** NO BACKFILL is enforced mechanically: the ledger refuses any row at or before `SHADOW_START`. The date is **not** moved backward because ≥2026-08-01 data now exists. **Reading the accumulated rows for a verdict requires its own preregistration**; incumbent first-reads stay governed by `LOCKED_FORWARD.md` and the rows below. Frontier **rank 1** as of 2026-08-28. ✅ **STARTED 2026-08-30** — the `GENESIS_ShadowRunner` scheduled task has ingested daily at 17:10 ET since `INIT 2026-08-30T04:09:02` (see the row below). The only remaining constraint is the **2026-09-01 18:00 ET boundary at which the ledger begins ACCEPTING rows.** ⚠️ Also update this row's parenthetical: the runner emits no orders and its `TARGET_ACCOUNTS` allowlist **excludes the live account `2047681`** — a real-money book now exists on this box, so that allowlist is load-bearing rather than theoretical. |
| **2026-11-01** | MONITOR-01 reading #2 (quarterly) | Frozen campaign-1 champion (R5-E10, 13 members; live-ops default = 16:44-flatten v2) | `MONITOR01_PROTOCOL.md`; alarm: σ-banded r < 1.05 two consecutive quarters, or banded drift beyond protocol bounds | Requires one fresh engine-exact 1-min NQ bar export (consumes nothing). Same reading also owes: SM13 B-MOM decay statistic (`../system_master/SM13_BMOM_DECAY_RULE.md`, floor $60/day rolling 504-session mean); CURRENT_EDGE_HEALTH refresh both products; re-check Product-A 10-13-contract band POSSIBLE_DECAY flag (escalate to STRUCTURAL_BREAK_EVIDENCE if still present at ~1,000 bars, else downgrade); Product-B rolling-120 Sharpe WATCH flag (should mean-revert as Jan-May 2026 rolls out). |
| quarterly thereafter | MONITOR-01 #3, #4, … | same | same | Standing cadence. |
| **🔴 ROLL DEADLINE — XM STOPS ENTERING 2026-09-06, P1 2026-09-08 (RESOLVED FROM NT8, NOT ESTIMATED)** | ⚠️ **CONTRACT ROLL — redeploy both paper legs on NQ 12-26.** VERIFIED from NT8 docs: *"NinjaScript strategies are not rolled forward and must be manually rolled over."* Nothing auto-rolls. ⛔ **ALL instruments must roll together — FOUR on the paper book, FIVE on the live book (MNQ is the fifth).** Any roll instruction naming only NQ/ES/RTY/YM is **INCOMPLETE** for account `2047681`. ⭐ **RESOLVED FROM NT8 AT THE 2026-08-30 18:00:00 ET SESSION OPEN** — no longer an estimate. The hardened classes refuse **NEW ENTRIES** from `stored rollover − RollLeadDays(8)` (`WeeklyEdgeP1PCT_v2.cs:460`), and the first realtime bar logged the resolved plan:<br>· `P1  ROLL-PLAN blockNewEntriesFrom=2026-09-08  earliestStoredRollover=2026-09-16 [NQU6:09-16]`<br>· `XM  ROLL-PLAN blockNewEntriesFrom=2026-09-06  earliestStoredRollover=2026-09-14 [NQU6:09-16 ESU6:09-14 RTYU6:09-15 YMU6:09-18]`<br>⇒ **XM binds FIRST, on 09-06, because ES rolls earliest (09-14)** — the multi-series leg takes the MIN across all four.<br>🔴 **"ROLL BY 2026-09-04" IS WITHDRAWN — IT WOULD HAVE PERMANENTLY KILLED THE BOOK.** `ResolveRollDates` runs ONCE on the first realtime bar and `RollBlocked()` is MONOTONE, and `GetNextRolloverDate` is a **ROOT-level** lookup that cannot tell you already rolled. Re-enabling P1 anywhere in **09-08 → 09-16**, or XM in **09-06 → 09-18**, recomputes the SAME block date and blocks **instantly and permanently** — while still reading Enabled/Realtime/bars-advancing/warm-up-GO. ✅ **SAFE RE-ENABLE: BOTH LEGS on/after 2026-09-19.** 🔴 **"P1 on/after 09-17" is WITHDRAWN 2026-09-01** — it came from NQ's 09-16 rollover alone; the LIVE P1's `MNQ` series rolls **09-18** and the guard takes the MIN over all series, so a 09-17 restart resolves `blockFrom = 09-10` and latches. The quoted ROLL-PLAN lines above are the **PAPER** book's; the LIVE lines are `P1 [NQU6:09-16 MNQU6:09-18]` and `XM [NQU6:09-16 ESU6:09-14 RTYU6:09-15 YMU6:09-18 MNQU6:09-18]`. Cost of waiting is **ZERO** (nothing enters between 09-08 and 09-19 either way); rolling early forfeits everything. Accept a ~10-day new-entry gap and record it in the shadow ledger so it is never read as a signal drought. Full analysis + pre-roll work (P1's `ExpectInstrument` is DISABLED; December minute history does not exist locally) in **`NT8_OPERATING_MODEL.md`**. **EXITS ARE NOT GATED**, so a missed roll stops new risk rather than stranding a position. Procedure: **`research/operational/CURRENT_LIVE_TRUTH.md` §ROLL is the SINGLE authority** (two authorities is how they drifted apart); everything else is a pointer. ✅ **CORRECTED 2026-09-01: `NT8_RUNBOOK.md`'s ROLL PROCEDURE is CURRENT and is the only complete step list** — it now carries `≥ 2026-09-19`, the full five-series parameter set, the `MNQZ6` acceptance check and the abort-on-`ROLL-PLAN` rule. Its old "roll by 2026-09-10" header is withdrawn **in place**. This row previously said to discard it, which would have sent the owner into the redeploy without the parameter list. | 🔴 **LIVE `2047681`: `WeeklyEdgeP1PCTMnq_v1` (399562885) + `WeeklyEdgeXMConflictMnq_v1` (399562886)** — **FIVE** series; must roll **`NQ 12-26` AND `MNQ 12-26`**; set `ExpectInstrument="NQ 12-26"` **and** `ExpectMnq="MNQ 12-26"` (the MNQ default is 09-26; `MxInstrumentGuard` hard-halts on a month mismatch); `MnqPerNq` — 🔴 **whatever the owner sets. The size is an OPEN decision** (OQ-7, `runs/CAP01B_RUIN_CORRECTION_20260901/`: at 3 MNQ the 2-year probability of losing the account is 6.5 %–22 % depending on whether the edge is real). Carry the current value forward only if it is unchanged; **the agent must not choose it.** **PAPER `DEMO8383477`: `WeeklyEdgeP1PCT_v3` + `WeeklyEdgeXMConflict_v4`** — four series. **Both books roll independently; verify both.** | `research/operational/CURRENT_LIVE_TRUTH.md` §ROLL | XM's `EsInstrument`/`RtyInstrument`/`YmInstrument` must be set to **ES/RTY/YM 12-26** at redeploy (they default to 09-26). ✅ The root-only guard bug is **FIXED in `_v3`** (it now checks the contract month), so a partial roll is caught rather than trading Dec NQ against Sept secondaries silently — but roll all four anyway. Flatten before swapping; record the swap in the deployment file. **The exact block date resolves on the first realtime bar and is logged as `ROLL-PLAN blockNewEntriesFrom=…` — read it from the NT8 log and pin this row.** |
| **daily 17:10 ET (automatic)** | `GENESIS_ShadowRunner` scheduled task ingests the paper stream into the hash-chained ledger | P1/PCT · XM_v2 on DEMO8383477 | `PROSPECTIVE_SHADOW.md` (2026-08-30 amendment) | ⚠️ **Logging is not reading.** Reading the ledger for a verdict still requires its own preregistration. |
| **2026-11-01** (with MONITOR-01 #2) | **HTFDIR01 + LIQREV01 shadow readings** — ⚠️ **RESTORED 2026-08-28 by GENESIS Wave 1**: this owner-authorized entry (commit `1093ba3`, protocol in `../system_master/CURRENT_TRUTH.md` §"shadows") had been **silently dropped** from this calendar during a rewrite. LIQREV01 passed **all 8 preregistered gates** ($579/trade, N=455) and was frozen out under the pre-W115 regime-veto doctrine the owner later revoked. | shadow protocol per system_master CURRENT_TRUTH; evidence `runs/GENESIS_W1_FORENSICS_20260828/reports/a2_git_forensics.md` | Restoration adds no new authority — it re-records an authorization that already existed. Any verdict read still needs its own preregistration. |
| **2027-08-01** | Annual frozen-champion locked-forward evaluation | R5-E10 (FROZEN definition only) | `LOCKED_FORWARD.md`; must be preregistered before the read; challenger comparisons need their own written protocol first | First full-year virgin-data evaluation. |
| **2027-08-01** | MONITOR-02: Program-B combined re-read | B-MOM + B-FADE + B1-overnight (all PARKED, 0 frozen) | **`MONITOR02_PROTOCOL.md`** (frozen 2026-08-18, before any forward read) | ≥12 months forward accrual; evaluation, not selection; unparking ≠ promotion. |

## Gated (no calendar date — a condition, authorization, or data volume unlocks them)

| Gate | Item | Governing doc |
|---|---|---|
| Owner authorization of §10(a) or (b), then ≥60 paired new sessions | SYSTEM_MASTER **B1 challenger** (drop-HTF Product B) future confirmation — resolves the INCONCLUSIVE Sharpe margin | `../system_master/B1_FUTURE_CONFIRMATION_SPEC.md` |
| Materially more tick/BBO sessions accumulated (archival currently idle — see OWNER_QUEUE §OQ-4) | U9/U9B microstructure re-test (frozen prequential design, larger sample) | `../system_master/ACTIVE_RESEARCH_QUEUE.md` (EVI rank 3) |
| Owner decision on NT8 export cost + new frozen AMENDMENT_3-style bundle with power analysis | AUCTION02 larger confirmation batch (160/168 protected sessions untouched) | `../system_master/` AUCTION02 ledger rows + `PROTECTED_EVIDENCE_BUDGET.md` |
| Explicit recorded owner re-authorization + improved resource architecture | DOM01 collection restart → eventually DOM-M1 discovery read | `../data_forward_sealed/DOM01/DOM01_DATA_GOVERNANCE.md`; pause: `../system_master/DOM_PAUSE_CLEANUP_20260812.md` |
| Explicit owner re-authorization (probe dates aging out ~90-day server window — OQ-4) | DATA03 Market Replay probe/batch acquisition | `runs/DATA03_HISTORICAL_MARKET_REPLAY_INVENTORY/acquisition_plan.yaml` |
| Owner purchase decision (~$80-199/mo) | GAMMA00 options/dealer-state data → re-open that feasibility lane | GAMMA00 ledger row (`../system_master/TESTING_LEDGER.csv`) |
| Preregistered spec + full gate battery (READY now — highest-EVI actionable item) | Direction-conditioned HTF construction | `../system_master/ACTIVE_RESEARCH_QUEUE.md` rank 2; evidence `HTFMECH01_TILT_MECHANISM/REPORT.md` |
| A frozen preregistered family showing ≥ +7pp on discovery AND (forward-data) holdout | Zone-F scalp reopening | `../scalping_lab/reports/ZONE_F_FINAL_VERDICT.md` (+ 2026-08-18 post-verdict note: holdout leg must use ≥2026-08-01 data) |


## Added 2026-08-27 — campaign #7 WEEKLY_EDGE forward reads

| Gate | Item | Governing doc |
|---|---|---|
| Sealed sessions from **2026-08-01** accumulate (virgin; no refit needed, the rule is parameter-free) | **`FOLLOW_MORNING` forward read** — W114 found it `REGIME_LOCAL`: 98.9th percentile on 2022-07 → 2026-08 but a 16-year out-of-window FAIL whose cause is behavioural, not cost (implied directional edge 0.70 % in 2006-21 vs 5.62 % now). The forward data is the only thing that can decide whether intraday momentum genuinely strengthened or the modern window is a twenty-year excursion. Read: is $/trade > 0 on sealed sessions, at the frozen 11:48 → 15:44 geometry, size 1, no stop, unchanged cost model. | `runs/WE_W114_INTRAMOM/REPORT.md` |
| Sealed sessions from **2026-08-01** accumulate | **`XM_CONFLICT` correlation watch** — W110 raised it to ACTIVE COMPONENT on the finding that the +0.464 six-month rho is NOT downside coupling (rho\|P1<0 = −0.165 at the 5.2nd percentile of a circular-shift null). The quantity to watch is the trailing 26-week rho **and** P(XM<0\|P1<0), which rose 0.200 → 0.500 between the first and last 26 weeks on ~11 P1-losing weeks. | `runs/WE_W110_XMDIVERSE/REPORT.md`, `../weekly_edge/CURRENT_BASELINE.md` |

## Standing per-wave (no date — every wave, mechanical)

- Seal audit: `src/analytics/seal_audit.py` — verdict pasted into each wave report (W18/W19 CLEAN precedent).
- Preregistration guard: `research_sdk/prereg_guard.py` — spec commit strictly before result commit.
- Boundary math: `research_sdk/session_boundary.py` — compute every backtest/read window against `LOCKED_FORWARD.md` before data is touched; canonical constants live there (dev end 2026-05-29; last consumed session 2026-07-31).

## Standing invalidation triggers (data-triggered, not scheduled)

- B-MOM decay below SM13 floor ($60/day rolling mean) → decision per SM13.
- Product invalidation criteria in `research/archive/campaign3_system_master/BASELINE_MODELS.md`
  ("what would invalidate" per object). _Moved from repo root 2026-08-27; campaign #3 is closed._
- CME/broker schedule changes (margin windows, early-close calendar) → re-check C4 compliance assumptions.
