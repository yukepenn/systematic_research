# FINAL_NQ_SYSTEM_RED_TEAM — Adversarial Review of the Program's Own Conclusions

_2026-08-08. Self-administered red team; standing objections that survive and must be
carried with the result. Nothing here was resolved away — these are the honest holes._

## 1. The composition was assembled, not preregistered as a whole
F5 = TILT × PORT_532 composes two independently-gated components, but the COMBINATION
was first evaluated in SM09 (measurement) and never had its own preregistered
promotion gate. Mitigation: both components passed frozen gates separately; the
composition's holdout read was protocol-frozen. Residual risk: mild selection at the
composition layer (2-3 combinations examined). VERDICT: disclose, downgrade F5's
formal status to "candidate composition of promoted parts" — F2 and PORT_532 carry
the clean pedigrees.

## 2. SM08's effect size invites suspicion
ΔSharpe +0.17 at P≤0.0005 from one binary state is unusually strong for this program's
history of nulls. Checks that held: two lengths (plateau), all 5 dev years, dead-regime
flip (constants not fitted anywhere), implementable form survives, mechanism matches
the independently-derived DD taxonomy.残 risks: (a) the tilt's edge concentrates in
the 2024-2026 counter-trend-short bleed — if that microstructure changes, the tilt
decays to neutral (holdout already showed neutral in a V-recovery); (b) linear P&L
scaling ≈ fractional contracts — integer quantization already cut the effect from
+25% to +9.7% net; live rounding at small f cuts further. VERDICT: real by every test
available; expect the SMALL implementable form, not the linear headline.

## 3. Two of three portfolio legs are regime-local (the program's own finding)
SM06 made the regime symmetry explicit. The 22.5%/yr frontier number is a
current-regime artifact by construction; the honest long-run expectation includes
regime death (2006-2017 Solar: leverage cannot rescue it). The B1 leg (structural but
weak, 53% of its 20-year edge in 10 nights) is thin insurance. VERDICT: the system's
true риск model is "edges die"; the monitors are load-bearing, not decorative.

## 4. Holdout power was ~zero and one-sided
45 sessions in the single best Solar window ever: it could not have falsified anything
except a sign bug. The tilt increment (+$244) is uninformative either way. VERDICT:
holdout = characterization only, as preregistered; no claim upgrades from it.

## 5. B-MOM's ledger is in-sample-adjacent
The W8-1 rule was frozen on the same 2022+ era it scores on; W10 proved no pre-2022
structure. SM05's gates measure portfolio value, not persistence. The decay monitor is
the only defense. VERDICT: stands (inherited, disclosed).

## 6. Costs and mechanics
B1 at 2.0t friction (not C1 2.872t; not significant at C1 in W9-1 — the B1 leg's
standalone edge is fragile); master-level rounding/tracking error unmeasured until the
parity run; the multi-leg master's combined same-day gross exposure cap is specified
but untested in-engine; NT8 master for F5 is SPEC + certified Python twins, not yet
compiled source (E10Master_v2 covers F1 today; F2 delta is ~40 lines). VERDICT: Track-8
engineering remainder is explicit and bounded.

## 7. Session-limit interruption
Wave-2 subagents died on an external usage limit; all five jobs were re-executed in
the main loop with identical frozen specs — no analytical impact, noted for the audit
trail.

## 8. What would change the verdict
(a) MONITOR-01 banded r < 1.05 two consecutive quarters → freeze deployment intent;
(b) B-MOM rolling-2y mean below its preregistered floor → drop the leg, weights renorm
to 0.7/0/0.3 — this contingency is NOT yet a frozen rule and must be preregistered
before ≥2026-11 monitoring; (c) MNQ fee +$0.10/side → E10 executable gate fails →
re-audit; (d) any future overlay idea that fails the 90% tail-retention gate is dead
on arrival regardless of its headline.
