Received 2026-08-08 (same day as V1/V2; V3 never issued — owner numbering goes V2 → V4).
Preamble (verbatim): "I grant to all the permisions to do al the things, you what is best for us."

================================================================================
OWNER MASTER DIRECTIVE V4
AUTONOMOUS NQ SYSTEMATIC RESEARCH PROGRAM
DRAWDOWN-FIRST · CURRENT-REGIME · MULTI-ENGINE · ONE-CONTRACT
CAUSAL STATE MODELS · ML/DL · PORTFOLIO ENGINEERING · NINJATRADER PARITY
================================================================================

ROOT:

D:\OneDrive - Washington University in St. Louis\TradingResearch\systematic_research


THIS DIRECTIVE IS THE CURRENT GOVERNING MASTER PROGRAM.

It does NOT erase prior evidence.

It inherits and consumes:

    Solar campaign
    Scalping Lab
    Program-B
    SYSTEM_MASTER V1
    SYSTEM_MASTER V2
    all SM / SMV2 runs
    all negative results
    all corrected errors
    all deep-research outputs
    all execution audits
    all current NinjaScript implementations
    all current registries and claim ledgers

as historical evidence.

Do NOT restart completed campaigns.

Do NOT rerun completed work merely because it appears in this directive.

Before execution:

    READ THE CURRENT LOCAL REPOSITORY.

If a task below has already been completed by a later commit:

    verify the artifact,
    record it as satisfied,
    and move immediately to the next highest-EVI task.

THE LOCAL COMMITTED REPOSITORY IS THE SOURCE OF TRUTH.

Older prompts, chat summaries, GitHub rendered pages, and files named FINAL
do NOT override a later committed correction.


================================================================================
0. OWNER AUTHORIZATION AND HARD BOUNDARIES
================================================================================

Continue AUTONOMOUSLY.

Do NOT ask the owner routine questions.

Do NOT stop after writing a status report.

Do NOT wait for:

    November
    future monitoring dates
    future data accumulation
    realtime observations
    simulated trading
    paper trading.

The owner wants the strongest historically defensible system that can be
built NOW from available historical information.

AUTHORIZED historical-research actions inside this repository include:

    read/write source code
    Python research
    NinjaScript creation/editing
    NinjaScript historical compilation
    NinjaTrader Strategy Analyzer historical backtests
    CrossTrade historical Strategy Analyzer tools
    bounded parameter studies
    causal feature engineering
    statistical analysis
    machine learning
    deep learning where justified
    /deep-research
    /dynamic-workflows
    research subagents
    worktrees/branches where useful
    documentation cleanup
    curated commits
    pushing validated research commits.

NOT AUTHORIZED:

    real orders
    simulated orders
    Sim101
    Playback trading
    realtime market-data recorder
    realtime strategy research
    live strategy activation
    brokerage account interaction
    account flatten/reverse
    credential changes
    license changes
    purchase of data/software
    destructive git history changes
    force-push
    protected vendor-binary decryption/bypass/modification/distribution.

CrossTrade may be used for:

    historical compilation
    strategy inspection
    Strategy Analyzer
    result retrieval
    effective-parameter inspection

only.

Every NinjaScript produced by this program must FAIL CLOSED for realtime
execution unless the owner later explicitly changes the mandate.


================================================================================
1. SUPREME OBJECTIVE
================================================================================

We are NOT optimizing one strategy.

We are building the strongest NQ/MNQ SYSTEMATIC SYSTEM supported by the
historical information presently available.

The desired architecture may ultimately be:

    Solar directional-persistence engine
            +
    B-MOM / other momentum engine
            +
    independent balance / failed-persistence / auction engine(s)
            +
    causal regime / viability state
            +
    risk / drawdown architecture
            +
    portfolio allocator / router
            +
    consolidated NinjaTrader execution engine.

PRIMARY OBJECTIVES:

    HIGHER ROBUST SHARPE

    HIGHER CALMAR

    LOWER TRUE BAR-MTM DRAWDOWN

    LOWER CDaR / EDaR / EXPECTED SHORTFALL

    SHORTER TIME UNDER WATER

    BETTER WORST WEEK / MONTH / QUARTER

    HIGHER POSITIVE-WEEK %

    HIGHER POSITIVE-MONTH %

    LOWER LOSING-MONTH STREAK

    BETTER ROLLING 20D / 60D / 120D FLOOR

    PRESERVED RIGHT-TAIL CONVEXITY

    HIGHER ROBUST GEOMETRIC GROWTH

    REALISTIC COSTS

    REALISTIC EXECUTION

    EXACT NINJATRADER REPRODUCIBILITY

    LOW ENOUGH COMPLEXITY TO UNDERSTAND AND MAINTAIN.

The fundamental objective is:

    MAXIMIZE RETURN PER UNIT OF PAINFUL RISK.

NOT:

    maximize backtest Net Profit.


================================================================================
2. CRITICAL SCIENTIFIC RULE — AGGRESSIVE SEARCH WITHOUT MANUFACTURING A WIN
================================================================================

Research AGGRESSIVELY.

But NEVER require every wave to beat the current champion.

That would guarantee eventual overfitting.

The correct loop is:

    generate a genuinely new mechanism
        ↓
    preregister a bounded family
        ↓
    test causally
        ↓
    promote if it survives
        OR
    kill honestly
        ↓
    retain current champion if challenger fails
        ↓
    move to another INDEPENDENT mechanism.

The system frontier must improve because INFORMATION improved.

Not because enough configurations were tried.

A legitimate research outcome is:

    CURRENT CHAMPION RETAINED —
    NO ROBUST IMPROVEMENT FOUND IN THIS FAMILY.

That is success.

The program must be:

    RELENTLESS ABOUT SEARCH

and simultaneously:

    RELENTLESS ABOUT FALSIFICATION.


================================================================================
3. PRACTICAL PRODUCTS — DO NOT FORCE THEM TO BE THE SAME STRATEGY
================================================================================

Maintain FOUR distinct products.

------------------------------------------------------------------------
PRODUCT A — BEST DAY-ONLY GRADED PORTFOLIO
------------------------------------------------------------------------

Can use graded MNQ/NQ exposure.

Must:

    remain day-only;
    use one consolidated target;
    be flat before the NinjaTrader margin cliff;
    have exact Strategy Analyzer implementation.

------------------------------------------------------------------------
PRODUCT B — BEST EXACTLY-ONE-CONTRACT STRATEGY
------------------------------------------------------------------------

Final action space:

    {-1, 0, +1}

for exactly:

    1 NQ
or
    1 MNQ.

No pyramiding.

No hidden fractional position.

It may use many virtual states internally.

It does NOT have to imitate Product A.

------------------------------------------------------------------------
PRODUCT C — SIMPLE ROBUST SYSTEM
------------------------------------------------------------------------

The least complex architecture retaining approximately 90–95% of the
robust economic benefit of Product A.

------------------------------------------------------------------------
PRODUCT D — BEST PORTFOLIO-OPTIMAL SOLAR COMPONENT
------------------------------------------------------------------------

The best Solar representation INSIDE the total system.

This may differ from:

    the highest-net standalone Solar.


================================================================================
4. FIRST ACTION EVERY SESSION — ESTABLISH REPOSITORY TRUTH
================================================================================

Run:

    git branch --show-current
    git rev-parse HEAD
    git status --short
    git log --oneline --decorate -50

Then read in this order:

    research/system_master/START_HERE.md
    research/system_master/CURRENT_TRUTH.md
    research/system_master/SYSTEM_SCORECARD.md
    research/system_master/CLAIM_LEDGER.md
    research/system_master/KNOWN_ERRORS_AND_CORRECTIONS.md
    research/system_master/SUPERSEDED_CONCLUSIONS.md
    research/system_master/NEXT_RESEARCH_QUEUE.md
    research/system_master/RESEARCH_INDEX.md
    research/system_master/EXECUTION_REALITY.md
    research/system_master/SYSTEM_FRONTIER.yaml

Then inspect relevant:

    run specs
    Python sources
    NinjaScript sources
    parity artifacts
    model artifacts
    deep-research reports
    rejected-idea ledgers
    registries.

If CURRENT_TRUTH disagrees with a number in this directive:

    CURRENT REPOSITORY WINS.

Do not blindly rerun an already completed task.

Do not silently trust an old result either.

Check its artifact provenance.


================================================================================
5. PROVISIONAL CURRENT OBJECT BOARD
================================================================================

VERIFY THIS AGAINST LOCAL HEAD.

Until contradicted by newer committed evidence:

SOLAR_E10

    established historical Family-A reference.

    13-member adaptive Solar directional-change ensemble.

    graded target.

    regime-local historically.

SOLAR_DUAL_HTF

    current improved Solar component.

    Solar ensemble
        +
    prior-session HTF agreement exposure tilt
        +
    counter-HTF short reduction.

BMOM_E2

    frozen B-MOM current-regime challenger/engine.

    noise-band + RTH VWAP continuation structure.

    causal next-bar execution audit passed historically.

    day-only.

DAYONLY_DUAL6040

    current graded day-only portfolio challenger.

    approximately:

        60% SOLAR_DUAL_HTF
        40% causal B-MOM

    on its stated risk basis.

    Must NOT be treated as executable champion unless consolidated
    NinjaTrader master parity has passed in CURRENT_TRUTH.

SM14_ONELOT

    old parity-tested one-contract reference.

A_DOMINANT_ONELOT

    one-contract challenger:

        B-MOM receives priority;
        Solar participates only at sufficiently strong consensus.

    historically improved SM14 across a small neighborhood.

    previous replacement gate was narrowly missed.

    NOT final unless later confirmation has passed.

B1_OVERNIGHT

    experimental diversifier.

    demoted from CORE unless new evidence changes the conclusion.

If the repo has advanced beyond these labels:

    update this board immediately.


================================================================================
6. CURRENT REGIME IS PRIMARY
================================================================================

This system is explicitly designed for the CURRENT tradable NQ regime.

Primary economic research universe:

    approximately 2022 → latest historical data available

subject to the actual CURRENT_REGIME definition in the repository.

Do NOT maximize pooled 2005–2026 performance blindly.

Older history is an:

    ADVERSARIAL STRESS SET

used to answer:

    Does the mechanism reverse?

    Does the strategy catastrophically churn?

    Was the modern edge absent?

    Can a CAUSAL viability state detect the difference?

    Is the system merely fitting calendar years?

Old-regime profitability is NOT automatically required for a
current-regime system.

But old-regime failure must NEVER be hidden.

Classify mechanisms honestly:

    STRUCTURAL
    REGIME_LOCAL
    UNKNOWN
    FAILED.

Never use:

    year >= 2022
    post-2020

as a deployable regime signal.

Find CAUSAL observable state variables.


================================================================================
7. THERE IS NO PRISTINE GLOBAL HISTORICAL OOS
================================================================================

Do NOT call June/July 2026:

    clean OOS
    pristine OOS
    untouched global holdout.

Previous research already consumed broad historical information.

Therefore current scientific validation relies primarily on:

    nested chronological validation
    rolling walk-forward
    expanding-window evaluation
    leave-one-year-out
    leave-one-quarter-out
    leave-half-year-out
    block bootstrap
    regime stress
    broad parameter plateaus
    explicit trial accounting
    implementation parity.

If CURRENTLY untouched historical data already exists:

    freeze the candidate first;
    consume it once if scientifically valuable;
    mark it consumed;
    never retune while pretending it remains untouched.

Do NOT wait for future data.


================================================================================
8. DRAWDOWN IS THE #1 SYSTEM PROBLEM
================================================================================

The program must continue to attack drawdown.

The owner does NOT accept unnecessarily large multi-month DD.

But:

    DO NOT DELETE ALL RISK
    merely to hit a dollar target.

Stretch levels such as:

    $18k
    $15k
    $12k

are research reference points.

They are NOT optimization constraints.

Any DD improvement must be measured under:

    native risk
    equal volatility
    equal exposure
    equal risk capital
    equal expected shortfall
    equal CDaR

where relevant.

Lower native dollar DD from taking less risk is NOT alpha.

The central question is:

    CAN WE REMOVE PAINFUL RISK
    WITHOUT REMOVING THE ECONOMIC EDGE?


================================================================================
9. TRUE DRAWDOWN METRICS
================================================================================

For every serious system report:

    EOD max DD

    true BAR-LEVEL MTM max DD

    CDaR

    EDaR

    Expected Shortfall

    Ulcer Index

    Pain Index if useful

    average DD

    95th-percentile DD

    maximum underwater duration

    median recovery

    95th-percentile recovery

    worst day

    worst week

    worst month

    worst quarter

    rolling 20D minimum

    rolling 60D minimum

    rolling 120D minimum

    positive-day %

    positive-week %

    positive-month %

    positive-quarter %

    longest losing-week streak

    longest losing-month streak.

Do NOT optimize:

    every day green
    every week green
    every month green.

That is unrealistic and can create pathological payoff distributions.

But PnL smoothness is a FIRST-CLASS objective.


================================================================================
10. DRAWDown CAUSAL DECOMPOSITION
================================================================================

Maintain:

    DRAWDOWN_CAUSAL_DECOMPOSITION.md

for:

    Solar
    current graded portfolio
    current one-contract challenger.

For every major DD classify dollars/bar-path effects into:

    LOSER ACCUMULATION

    WINNER ABSENCE

    SOLAR LOSS

    BMOM LOSS

    JOINT ENGINE LOSS

    OPPOSITE-SIGN CONFLICT

    COMMON-REGIME FAILURE

    LONG LOSS

    SHORT LOSS

    HTF AGREEMENT

    HTF CONFLICT

    LOW CONSENSUS

    HIGH CONSENSUS

    MORNING

    MIDDAY

    AFTERNOON

    LOW OPPORTUNITY SCALE

    HIGH FLIP / INSTABILITY

    LOW DIRECTIONAL EFFICIENCY

    EXECUTION COST

    POSITION SIZE

    ONE-CONTRACT DISCRETIZATION

    OTHER.

Where practical use counterfactual/Shapley-style decomposition for:

    graded sizing
    HTF tilt
    conditional short reduction
    B-MOM addition
    B1 removal
    portfolio netting
    Engine #3
    routing.

Eventually answer:

    Of the original Solar DD,

    how much was removed by each mechanism?

    What remains?

    Why does it remain?

    Is remaining DD structurally necessary compensation for the right tail?


================================================================================
11. RIGHT-TAIL PRESERVATION
================================================================================

Solar is strongly right-tail dependent.

Every:

    stop
    filter
    state gate
    router
    ML overlay
    side reduction
    allocation policy

must report:

    Solar top-10-day retention
    Solar top-1%-trade retention
    largest-winner retention
    trend/crisis convexity
    skew change
    total-system right-tail replacement.

Default warning levels:

    <90% retention:
        investigate carefully.

    <85%:
        promotion caution.

    <75%:
        normally reject,

UNLESS an independent return engine materially replaces the lost tail and
improves total robust geometric growth.

Do not approve a beautiful equity curve created by amputating the strategy.


================================================================================
12. STOP / EXIT RESEARCH — EVIDENCE-DRIVEN ONLY
================================================================================

Many conventional stop classes have already failed.

Do NOT rerun identical:

    hard stop
    timed exit
    post-loss throttle
    split exit
    progress stop

without a genuinely new mechanism.

The prior evidence suggests major DD improvements came more from:

    graded sizing
    HTF conditioning
    side conditioning
    portfolio diversification

than tighter trade stops.

But do NOT conclude:

    ALL stops are impossible.

New stop/exit work is allowed ONLY when DD anatomy creates a causal reason.

Potential distinct mechanisms:

    catastrophe protection
    structural invalidation
    causal no-progress + adverse-state exit
    dynamic trail after sufficiently large MFE
    event-risk protection
    engine-conflict exit
    conditional re-entry after protective exit.

Every protective exit must answer:

    what loser class is removed?

    which big winners are lost?

    is re-entry required?

    what is the portfolio-level effect?

No generic stop optimization matrix.


================================================================================
13. ONE-CONTRACT PRODUCT — INDEPENDENT CONTROL PROBLEM
================================================================================

Exactly-one-contract is NOT:

    "scale the full portfolio down."

It is a separate discrete-control problem:

    action ∈ {-1, 0, +1}.

Maintain both:

    1 NQ
    1 MNQ.

Virtual inputs may include:

    Solar vote
    Solar member hierarchy
    HTF
    B-MOM
    Engine #3
    viability state
    trend-quality state
    side state
    router state.

But final position is exactly:

    short one
    flat
    long one.

No pyramiding.

No averaging down.

No hidden fractional exposure.

Continue confirming/killing bounded policy families such as:

    B-MOM priority + strong Solar

    agreement only

    B-MOM priority

    Solar priority

    Solar only when B-MOM neutral/aligned

    B-MOM only when Solar confidence weak

    HTF-conflict flat

    side-dependent priority

    conflict persistence before switch

    causal viability chooses engine

    balance/trend router.

Do NOT hand-fit a 25-state decision tree.

Use small, interpretable action maps.


================================================================================
14. ONE-CONTRACT REALITY CHECK
================================================================================

One NQ is a coarse and large risk unit.

Do NOT pretend it is low risk.

For the best one-NQ system report capital required for historical DD equal to:

    10%
    15%
    20%
    25%
    30%

and after:

    1.25× historical DD stress
    1.50×
    2.00×.

Report one-MNQ separately.

If one NQ cannot achieve an acceptable robust DD frontier without
destroying its economics:

    SAY SO.

The scientifically correct one-contract practical product may be:

    ONE MNQ.

Do not torture the strategy to make one NQ look safe.


================================================================================
15. PRODUCT A — CONSOLIDATED DAY-ONLY MASTER
================================================================================

The leading graded portfolio must ultimately exist as:

    ONE NinjaTrader Strategy.

Conceptual architecture:

    virtual 13-member Solar ensemble
            ↓
    graded Solar target
            ↓
    HTF agreement conditioning
            ↓
    conditional short-risk shaping
            +
    virtual causal B-MOM
            +
    Engine #3 if promoted
            +
    causal router/state if promoted
            ↓
    frozen family weights/risk mapping
            ↓
    ONE CONSOLIDATED TARGET
            ↓
    ONE ORDER ENGINE.

No independent strategies fighting for account position.

No double counting.

No hidden leverage.

No overnight position in PRODUCT A.

The strategy must demonstrate that it is flat BEFORE the verified
NinjaTrader margin cliff.

Prove actual realized historical fill times.

Do not merely write:

    flat at 16:44.

Holiday / early-close behavior must be deterministic.


================================================================================
16. PYTHON ↔ NINJATRADER PARITY
================================================================================

Compilation is NOT validation.

Every finalist must obtain:

    research implementation
            ↕
    NinjaTrader Strategy Analyzer implementation

parity.

Log:

    member states
    engine states
    HTF state
    viability state
    desired target
    executed target
    target changes
    direction
    quantity
    entry timestamp
    entry price
    exit timestamp
    exit price
    commissions
    slippage
    realized daily PnL
    MTM daily PnL
    cumulative PnL
    DD path.

Default final parity targets:

    decision-path match >= 99.5%

    daily PnL correlation >= 0.999

    net difference <= 0.5%

    no economically material unexplained mismatch.

Exact timestamp-label differences that do not change price/economics may
be documented.

If executable performance is worse than Python:

    EXECUTABLE PERFORMANCE BECOMES THE HEADLINE.

Never preserve the prettier research number.


================================================================================
17. EXECUTION COST REALITY
================================================================================

Use the CURRENT repository execution conventions.

Do not silently invent cheaper friction.

Maintain explicit cost corridor such as:

    C0 — theoretical reference only

    C1 — standard historical benchmark

    C2 — realistic causal fill convention

    C3 — additional adverse execution stress

    C4 — stronger adverse execution stress

    C5 — severe liquidity/spread stress

using exact definitions from CURRENT_TRUTH / EXECUTION_REALITY.

B-MOM has already survived a causal execution audit historically unless
later artifacts contradict this.

Carry execution stress into the combined MASTER.

Analyze cost sensitivity by:

    time of day
    volatility
    macro events
    roll
    early close
    NQ vs MNQ
    engine
    turnover
    target rounding.

Do not promote a strategy whose superiority exists only under optimistic
cost assumptions.


================================================================================
18. VOL MATCHING / NORMALIZATION AUDIT
================================================================================

Every scaling operation must be classified:

    whole-sample hindsight
    dev-static
    fold-static
    expanding causal
    rolling causal
    engineering constant.

Final executable systems should use:

    frozen static scales
or
    explicitly causal scales.

No hidden full-period volatility knowledge.

No hidden PnL-reactive leverage.

No accidental:

    lose money → estimated vol rises → exposure falls

unless that exact causal policy was intentionally tested and shown to have
positive predictive value.

Portfolio performance must be recomputed using practical scaling.


================================================================================
19. PORTFOLIO WEIGHT FRONTIER
================================================================================

Never fine-optimize continuous weights.

Use coarse structural neighborhoods.

For Solar/B-MOM, examples:

    80/20
    70/30
    60/40
    50/50
    40/60

and only expand when evidence justifies it.

Evaluate independently by:

    Sharpe
    Calmar
    CDaR
    EDaR
    Expected Shortfall
    Ulcer
    worst month
    worst quarter
    TUW
    positive months
    robust log growth
    right-tail capture.

Use multiple chronology geometries:

    fit 2022–2024 → evaluate 2025–latest

    leave-one-year-out

    expanding chronology

    quarter blocks

    joint-loss block stress.

If many risk objectives point to roughly the same broad zone:

    confidence rises.

If optima diverge:

    preserve separate Pareto candidates.

Do NOT force one universal "best weight."


================================================================================
20. PORTFOLIO PARETO FRONTIER
================================================================================

Maintain separate champions:

    MAX_SHARPE

    MAX_CALMAR

    MIN_MAXDD

    MIN_CDAR

    MIN_EDAR

    MIN_ULCER

    MIN_TUW

    MAX_POSITIVE_MONTH

    MAX_ROBUST_GROWTH

    SIMPLEST_ROBUST

    BEST_DAY_ONLY

    BEST_ONE_NQ

    BEST_ONE_MNQ

    BEST_ONE_CONTRACT.

Do not collapse them prematurely.

A slightly lower-return strategy with much lower painful risk can dominate
after robust risk normalization.


================================================================================
21. CAUSAL EDGE-VIABILITY STATE — HIGH PRIORITY
================================================================================

One of the most important structural discoveries so far is:

    MARKET PERSISTENCE CAN EXIST
    WHILE AFTER-COST MONETIZATION FAILS.

Build an interpretable causal VIABILITY STATE.

Potential components:

OPPORTUNITY SCALE

    causal movement scale / all-in friction.

DIRECTIONAL QUALITY

    overshoot quality
    path efficiency
    variance ratio
    trend t-stat
    Solar consensus
    retracement quality.

INSTABILITY

    DC frequency
    flip frequency
    reversal rate
    vol-of-vol
    jump rate.

LIQUIDITY/COST

    spread
    expected slippage
    time-of-day liquidity
    roll.

Do NOT immediately fit a giant continuous score.

Start with broad states:

    UNTRADEABLE
    MARGINAL
    VIABLE.

Requirements:

    known before the trade;
    no future PnL;
    no current-trade outcome;
    no calendar-year switch;
    monotonic expected economics;
    broad thresholds;
    chronology stability;
    old-regime same-direction relationship where possible;
    right-tail preservation;
    NinjaTrader deployability.

Test diagnostic information FIRST.

Only then allow:

    0
    reduced
    normal

risk.


================================================================================
22. HTF / TREND-QUALITY RESEARCH
================================================================================

Do NOT assume MA crossover is the answer.

Solar already identifies direction.

A second lagging direction indicator can merely:

    confirm the same information later

and eat right-tail profits.

Research TWO distinct jobs.

JOB A — DIRECTION CONTEXT

Examples:

    SMA50
    SMA100
    SMA200
    EMA50
    long-horizon regression sign
    KAMA state
    large-threshold DC state.

Question:

    does slow direction change the expected value/risk of Solar?

JOB B — TREND QUALITY

Examples:

    Efficiency Ratio
    VHF
    ADX / DMI
    Choppiness
    Aroon
    Vortex
    regression R²
    normalized slope t-stat
    variance ratio
    path efficiency
    retracement quality
    Solar member hierarchy.

Question:

    IS THIS TREND CLEAN OR NOISY?

For every feature measure incremental information conditional on:

    Solar
    current HTF
    B-MOM.

If only one exact MA period works:

    downgrade confidence.

Do not select SMA73 because it wins by $4k.


================================================================================
23. SHORT-SIDE / SIDE-ASYMMETRY PROGRAM
================================================================================

The current conditional short reduction is promising.

But continue scientifically.

Separate:

    unconditional short reduction

from:

    HTF-conditioned short reduction

from:

    viability-conditioned short reduction

from:

    regime-conditioned side risk.

Evaluate:

    full shorts
    coarse reduced shorts
    long-only

only as broad mechanism tests.

Mandatory:

    worst 20 NQ days
    worst 50
    2022 selloff clusters
    gap-down days
    sharp reversal days
    crisis-day convexity
    portfolio-level compensation from B-MOM / Engine #3.

The correct objective is:

    TOTAL PORTFOLIO CRISIS PROTECTION,

not maximizing one Solar leg.


================================================================================
24. ENGINE #3 — GENUINELY DIFFERENT RETURN ENGINE
================================================================================

Solar and B-MOM both substantially monetize directional persistence.

The next transformative improvement should search for:

    FAILED PERSISTENCE

    BALANCED AUCTION

    VALUE REACCEPTANCE

    INVENTORY CORRECTION

    STRUCTURAL REVERSAL

rather than another renamed trend engine.

Priority families include:

ENGINE3-A
    failed range break → value reacceptance.

ENGINE3-B
    failed opening-range breakout.

ENGINE3-C
    small normalized gap fade.

ENGINE3-D
    overnight high/low failure during RTH.

ENGINE3-E
    prior-day high/low failure.

ENGINE3-F
    initial-balance failure.

ENGINE3-G
    VWAP reclaim/rejection with structural state transition.

ENGINE3-H
    session inventory liquidation.

ENGINE3-I
    post-shock normalization.

ENGINE3-J
    ES/NQ relative-value state.

ENGINE3-K
    gap continuation — SEPARATE family from gap fade.

ENGINE3-L
    balanced-auction rotation.

ENGINE3-M
    failed directional-persistence / range reentry.

ENGINE3-N
    event digestion / delayed reversal.

Do NOT combine fade and continuation into an unconstrained flexible
backtest that selects whichever side worked historically.


================================================================================
25. ENGINE #3 PROMOTION STANDARD
================================================================================

Engine #3 does NOT need B-MOM's standalone Sharpe.

It needs:

    positive after-cost standalone expectancy

    adequate observations

    causal implementation

    broad plateau

    chronology stability

    C1/C2 survival

    reasonable turnover

and, critically:

    GOOD PERFORMANCE WHEN THE CURRENT SYSTEM IS LOSING.

Measure:

    losing-day correlation with current champion

    losing-week correlation

    bottom-decile champion-day PnL

    PnL during champion DD episodes

    worst-month overlap

    joint tail loss

    common-regime failure overlap.

A standalone Sharpe 0.5–0.7 engine can be strategically excellent if it
meaningfully improves portfolio Calmar/CDaR/TUW.

Do not reject diversification alpha because raw return is lower.


================================================================================
26. ENGINE #4 AND BEYOND
================================================================================

Do NOT assume exactly three engines is optimal.

After Engine #3 search:

if a genuinely independent mechanism emerges:

    preserve it.

If multiple distinct positive families emerge:

    evaluate each independently first.

Only then combine them.

Potential future engines may arise from:

    auction
    volatility
    relative value
    session structure
    event response
    inventory
    cross-index
    causal state estimation.

Do not increase engine count simply for diversification optics.

Every engine must earn its complexity.


================================================================================
27. INDICATOR / FEATURE FACTORY
================================================================================

Continue aggressive feature discovery.

But:

    NO INDICATOR ROULETTE.

Every feature receives a hypothesis card:

    mechanism
    expected relationship
    information horizon
    decision timestamp
    required data
    incremental-information question
    falsification criterion.

Candidate families include:

TREND QUALITY

    Efficiency Ratio
    VHF
    ADX/DMI
    Aroon
    Vortex
    Donchian persistence
    regression slope
    regression R²
    normalized slope t-stat
    variance ratio
    causal persistence estimates
    path efficiency.

COMPLEXITY / CHOP

    Choppiness Index
    permutation entropy
    sample entropy
    spectral entropy
    sign-change frequency
    DC flip frequency
    run-length structure.

VOLATILITY

    realized volatility
    semivariance
    bipower variation
    jump variation
    vol-of-vol
    Parkinson
    Garman-Klass
    Rogers-Satchell
    compression
    expansion
    range/friction ratio.

STATE ESTIMATION

    Kalman local level
    Kalman trend
    innovation magnitude
    innovation whiteness
    CUSUM
    BOCPD
    HMM
    Markov switching
    change-point age.

ADAPTIVE / SIGNAL PROCESSING

    KAMA
    FRAMA
    VIDYA
    Ehlers SuperSmoother
    Roofing Filter
    Fisher Transform
    causal band-pass ideas
    dominant-cycle diagnostics.

AUCTION / LOCATION

    VWAP
    VWAP slope
    VWAP acceptance
    anchored VWAP
    opening range
    initial balance
    prior-day high/low
    overnight high/low
    prior close
    gap state
    value-area proxies
    volume-profile features if causally available.

CROSS-MARKET

    ES direction
    ES trend quality
    NQ/ES relative movement
    RTY/YM confirmation
    volatility index context where causal/free
    rates/macro context where justified.

Every feature proceeds:

    JOB 1 — INFORMATION

Does X add incremental information about:

    future Solar expectancy
    Solar failure probability
    big-winner probability
    MFE
    MAE
    B-MOM expectancy
    Engine #3 expectancy
    engine disagreement
    portfolio downside?

ONLY IF JOB 1 PASSES:

    JOB 2 — POLICY.

Do not turn every indicator into a standalone trading strategy.


================================================================================
28. DEEP RESEARCH
================================================================================

Use /deep-research repeatedly.

External research is a HYPOTHESIS GENERATOR.

It is never local proof.

Run independent mechanism-expansion passes such as:

PASS A
    academic quantitative finance.

PASS B
    futures practitioner research.

PASS C
    auction / market microstructure.

PASS D
    signal processing / control / state estimation.

PASS E
    downside-risk / portfolio construction.

PASS F
    NinjaTrader / systematic futures ecosystem.

PASS G
    ML / sequence modeling.

Each idea must return:

    economic mechanism
    observables
    direction
    horizon
    causal availability
    local data requirement
    falsification test
    likely duplication with prior killed families.

Then LOCAL DATA decides.

If empirical results reveal something new:

    branch research from that finding.

Do not make one literature review the final idea list.


================================================================================
29. MACHINE LEARNING FACTORY
================================================================================

Machine learning is fully allowed.

It is NOT a loophole around selection discipline.

Preferred first jobs:

    engine-quality estimation

    failure prediction

    regime classification

    viability estimation

    exposure allocation

    engine selection

    portfolio routing.

Do not begin with:

    unconstrained next-bar direction prediction.

Useful targets include:

MODEL-A
    P(next Solar episode profitable after realistic cost).

MODEL-B
    expected Solar episode return.

MODEL-C
    P(MFE reaches X before MAE reaches Y).

MODEL-D
    trend-failure hazard.

MODEL-E
    P(next B-MOM trade profitable).

MODEL-F
    next-session state:
        persistence
        failed persistence
        balanced
        shock/ambiguous.

MODEL-G
    expected return for:
        Solar
        B-MOM
        Engine #3.

MODEL-H
    conditional portfolio return/DD quantiles over:
        1
        5
        20 sessions.

MODEL-I
    choose among a SMALL approved policy set.

MODEL-J
    probability of joint Solar+B-MOM loss.

MODEL-K
    probability DD continues over next N sessions,
    using causal MARKET state rather than recent equity pain alone.


================================================================================
30. MODEL COMPLEXITY LADDER
================================================================================

Always start simple.

LEVEL 0

    unconditional baseline
    HTF rule
    consensus buckets
    viability buckets.

LEVEL 1 — INTERPRETABLE

    regularized logistic
    elastic net
    GAM
    monotonic GAM
    ordinal regression
    quantile regression
    survival/Cox
    isotonic calibration
    Bayesian hierarchical models.

LEVEL 2 — TREE ENSEMBLES

    XGBoost
    LightGBM
    CatBoost
    Random Forest
    Extra Trees
    histogram gradient boosting.

LEVEL 3 — STATE / MIXTURE

    HMM
    Markov switching
    Kalman/state-space
    mixture of experts
    Bayesian change point.

LEVEL 4 — SEQUENCE DEEP LEARNING

    TCN
    GRU
    LSTM
    small Transformer

only if earlier models establish real information.

LEVEL 5 — REINFORCEMENT LEARNING

Only if everything above produces a justified need.

RL restrictions:

    approved engine outputs only
    small fixed action space
    historical policy simulation only
    no raw unrestricted trading
    no account interaction
    interpretable/deployable policy
    must beat fixed/simple policies robustly.

Do not use model complexity as a substitute for edge.


================================================================================
31. ML DATA HYGIENE
================================================================================

Mandatory:

    all features known at decision time

    no future H/L

    no full-sample scaling

    no label leakage

    no overlapping-label leakage

    purge by max label/trade horizon

    embargo where appropriate

    hyperparameter tuning INSIDE inner folds

    outer chronological folds untouched until model freeze

    deterministic seeds

    causal missing-value handling

    imbalance handling within training only

    calibration measured out-of-fold

    predictions generated OOF for historical portfolio construction.

Track:

    every feature-family trial

    every model family

    every label change

    every hyperparameter search budget.


================================================================================
32. ML EVALUATION
================================================================================

Predictive diagnostics:

    log loss
    Brier
    calibration
    ROC-AUC
    PR-AUC
    rank correlation
    seed stability.

These are NOT promotion criteria.

Promotion requires:

    after-cost system benefit

    Sharpe improvement

    Calmar improvement

    CDaR/ES improvement

    chronology stability

    turnover control

    right-tail preservation

    feature stability

    model stability

    implementation feasibility

    superiority to simpler baselines.

If an interpretable model produces 90–95% of a complex model's robust
portfolio benefit:

    PREFER THE INTERPRETABLE MODEL.


================================================================================
33. DEEP LEARNING GATE
================================================================================

Do not use deep learning merely because it is available.

Deep models must beat:

    logistic/GAM
    tree ensembles

on robust OUTER chronology.

Require:

    stable seeds
    feature ablation survival
    cost survival
    period stability
    no one-year dependence
    deterministic frozen inference
    operational feasibility
    C# / runtime inference parity.

If tree models do not robustly improve over simple models:

    STOP BEFORE DL.

If DL fails:

    KILL IT.

Do not endlessly tune architectures.


================================================================================
34. ML META-ALLOCATOR / MIXTURE OF EXPERTS
================================================================================

Only after individual engines have standalone evidence.

Possible architecture:

    Solar expert
    B-MOM expert
    Engine #3 expert
            ↓
    OOF conditional-return / quality estimates
            ↓
    calibrated router
            ↓
    fixed family risk caps
            ↓
    consolidated target.

Model output initially may control only:

    engine eligible / not eligible

    0 / 0.5 / 1 risk

or:

    one policy from a small approved set.

No unconstrained continuous leverage optimizer.

No model gets to invent arbitrary position sizes.


================================================================================
35. REGIME ROUTER
================================================================================

Build a SIMPLE interpretable router before ML routing.

Potential states:

    PERSISTENCE

    FAILED_PERSISTENCE

    BALANCED_VALUE

    SHOCK_AMBIGUOUS

    NO_TRADE.

Potential inputs:

    Solar vote consensus
    HTF
    B-MOM
    directional efficiency
    overshoot quality
    retracement
    flip frequency
    opportunity/friction
    VWAP acceptance
    volatility
    jumps
    Engine #3 state.

Conceptual behavior:

PERSISTENCE

    Solar/B-MOM eligible.

FAILED_PERSISTENCE

    failed-break/value engine eligible.
    trend risk reduced.

BALANCED_VALUE

    auction/reversion engine eligible.
    trend engines limited.

SHOCK_AMBIGUOUS

    reduced/flat until causal acceptance.

NO_TRADE

    expected opportunity does not justify friction.

Compare:

    no router
    simple router
    logistic/GAM router
    tree router.

Do NOT fit dozens of state boundaries.


================================================================================
36. CROSS-MARKET CONTEXT
================================================================================

Final traded instrument remains:

    NQ/MNQ.

Context MAY use historical:

    ES
    RTY
    YM
    volatility indices
    rates
    macro calendar
    overnight session
    session handoffs

when:

    data is available
    provenance recorded
    timestamps causal
    missing behavior defined
    licensing permits.

Questions include:

    Does ES alignment improve NQ trend quality?

    Does NQ/ES divergence identify failed persistence?

    Does cross-index disagreement predict bad Solar entries?

    Does event state change gap/fade behavior?

Do not cherry-pick a single context market after viewing results.


================================================================================
37. COMMON-REGIME FACTOR
================================================================================

Solar and B-MOM may share modern directional-persistence fuel.

Build a causal common-regime analysis.

Candidate state variables:

    3m movement scale
    daily/RTH range
    opportunity/friction ratio
    directional efficiency
    trend-day frequency
    B-MOM activation
    Solar overshoot economics
    flip frequency
    volatility
    vol-of-vol.

Question:

    WHY DID BOTH IMPROVE IN THE MODERN REGIME?

Find market-state explanations.

Do NOT fit:

    "year is after 2020."

Then specifically seek Engine #3 behavior when common-regime quality is weak.


================================================================================
38. PORTFOLIO COMPLEMENTARITY
================================================================================

For every positive engine report:

    full daily correlation

    losing-day correlation

    losing-week correlation

    bottom-decile champion-day correlation

    joint ES

    drawdown overlap

    recovery overlap

    worst-month overlap

    right-tail overlap

    common-regime dependence.

Low full correlation alone is insufficient.

A useful engine should improve the periods that hurt the current portfolio.


================================================================================
39. LEVERAGE / COMPOUNDING — RESEARCH ONLY
================================================================================

Leverage is NOT alpha.

No leverage recommendation.

Only after a candidate is mechanically implemented and thoroughly audited,
run hypothetical risk-normalization research.

Use:

    moving blocks 5 / 20 / 60 / 120
    stationary bootstrap
    quarter blocks
    year blocks
    episode blocks
    joint-loss blocks

where statistically defensible.

Stress:

    correlation → 1
    spread ↑
    slippage ×1.5 / ×2
    winner drought
    trend-day frequency decline
    gap risk.

Use a stated capital base.

Report sensitivity at portfolio vol targets such as:

    6%
    8%
    10%
    12%

only as research scenarios.

Report:

    geometric return
    CAGR estimate
    Sharpe
    Calmar
    DD
    CDaR
    ES
    recovery
    probability DD > thresholds
    margin stress.

Headline should use:

    WORST DEFENSIBLE RESAMPLING METHOD,

not the prettiest one.

Never call bootstrap CAGR:

    expected future CAGR.


================================================================================
40. SCIENTIFIC MECHANISM CARD
================================================================================

Every promoted:

    strategy
    overlay
    indicator
    model
    router

must have a MECHANISM CARD answering:

    What exact market behavior is monetized?

    Who/what is the economic counterparty?

    Why might the behavior persist?

    What risk is being compensated?

    What psychology/institutional process supports it?

    When should it lose?

    What regime should break it?

    What observable variable would invalidate it?

    What part is true alpha?

    What part is beta?

    What part is exposure transformation?

    What part is diversification?

    What part is cost reduction?

Do not promote:

    "backtest got better"

as an economic explanation.


================================================================================
41. EXPERIMENT CLASSIFICATION
================================================================================

Every experiment receives one label BEFORE execution:

    AUDIT

    DIAGNOSTIC

    R1 FAMILY TEST

    R2 CONFIRMATION

    PORTFOLIO TEST

    MODEL TEST

    EXECUTION TEST.

Before every selection-bearing test freeze:

    experiment ID
    hypothesis ID
    parent family
    mechanism
    source commit
    data window
    features
    labels
    execution model
    costs
    parameter/model budget
    metric battery
    pass criteria
    kill criteria
    next action if pass
    next action if fail.

Registry counts must distinguish:

    audits
    diagnostics
    alpha trials
    portfolio trials
    model trials
    hyperparameter trials
    execution stresses.

A rename does NOT reset trial count.

A Python→C# rewrite does NOT reset selection count.

A changed feature set counts.

A changed model architecture counts.

A changed label counts.


================================================================================
42. HIERARCHICAL RESEARCH BUDGET
================================================================================

STAGE 1 — SCREEN

    6–12 coarse configurations normally.

STAGE 2 — CHARACTERIZE

    only if Stage 1 passes.

    12–30 neighboring configurations normally.

STAGE 3 — CONFIRM

    frozen family.

    chronology
    costs
    stress
    old-regime
    no new tuning.

STAGE 4 — IMPLEMENT

    NinjaTrader parity.

    NO ALPHA CHANGES.

No massive optimizer grid before Stage 1 evidence.


================================================================================
43. PARAMETER PLATEAU
================================================================================

Prefer:

    plateau center

not:

    historical argmax.

If:

    49
    50
    51

all work:

    promising.

If:

    53 works
    52 and 54 fail:

    fragile.

Report neighborhood medians and stability.

Never choose a bizarre exact setting solely for historical Net.


================================================================================
44. PROMOTION GATES
================================================================================

A serious candidate must pass relevant categories:

MECHANISM

    credible rationale
    explicit failure regime.

ECONOMICS

    after realistic costs positive.

CHRONOLOGY

    stable folds
    no single-year dependency.

PLATEAU

    broad neighboring support.

DRAWDOWN

    true MTM
    equal-risk comparison
    CDaR/ES/TUW.

RIGHT TAIL

    required retention.

PORTFOLIO

    incremental total-system value.

EXECUTION

    realistic causal costs
    severe stress understood.

COMPLEXITY

    simpler alternative does not capture nearly all value.

STATISTICS

    trial burden disclosed
    no moved gates.

PARITY

    deterministic research↔NinjaTrader reconciliation.

No promotion solely because:

    Net is higher

    AUC is higher

    native DD is smaller due to lower size

    trade count is lower

    one year is excellent

    one model seed is excellent.


================================================================================
45. KILL CRITERIA
================================================================================

Kill a SPECIFIC family when:

    after-cost expectancy negative

    no broad plateau

    chronology sign unstable

    result depends on one year

    edge disappears under realistic cost stress

    mechanism duplicates a killed family

    improvement is only trivial exposure reduction

    right-tail destruction excessive

    joint-loss behavior worsens

    simpler model matches it

    implementation nondeterministic

    model seed variance excessive

    old-regime behavior reveals catastrophic failure with no causal off-state

    repeated bounded variants fail and EVI becomes low.

Record kills permanently.

Do not revive under a new name without documenting the genuinely new mechanism.


================================================================================
46. DRAWDown STRETCH IMPROVEMENTS
================================================================================

If DD falls below a stretch threshold:

DO NOT celebrate immediately.

Red-team:

    Was risk simply reduced?

    Did average exposure fall?

    Was right tail deleted?

    Did one year cause it?

    Did volatility normalization leak future information?

    Did execution assumptions improve?

    Did portfolio weights overfit?

Only call DD improvement meaningful when:

    equal-risk economics improve.


================================================================================
47. SMOOTHNESS FRONTIER
================================================================================

The owner values:

    frequent realized gains
    smoother weeks
    smoother months
    shorter recovery.

Research that explicitly.

But never optimize win-rate/smoothness in isolation.

For every "smooth" candidate compare:

    average win
    average loss
    PF
    payoff ratio
    skew
    ES
    worst loss
    concentration.

Reject:

    80% wins
    tiny profit
    rare catastrophic loss.

A lower win rate with superior payoff and much lower left tail may dominate.


================================================================================
48. PARALLEL RESEARCH ORGANIZATION
================================================================================

Use /dynamic-workflows.

Potential specialists:

    COORDINATOR

    REPOSITORY_TRUTH

    NINJASCRIPT_MASTER

    ONECONTRACT_CONTROL

    DRAWDOWN_ATTRIBUTION

    REGIME_VIABILITY

    AUCTION_ENGINE

    GAP_ENGINE

    VALUE_REACCEPTANCE

    VOLATILITY_ENGINE

    CROSS_MARKET

    STATE_FEATURE_FACTORY

    ML_INTERPRETABLE

    ML_TREE

    ML_SEQUENCE

    PORTFOLIO_DOWNSIDE

    EXECUTION_REALITY

    STATISTICAL_REDTEAM

    DOCUMENTATION_INTEGRATOR.

Rules:

    isolated run folders

    no concurrent edits to core state-machine source

    registry writes centralized

    evaluation conventions centralized

    promotion centralized

    red team cannot be skipped

    no agent promotes its own candidate unilaterally.


================================================================================
49. THROUGHPUT — NO RESEARCH THEATER
================================================================================

Documentation is now infrastructure.

Maintain it.

Do not make it the output.

Every major wave must produce at least one:

    mechanical truth/correction

    real trade result

    killed mechanism

    promoted challenger

    DD improvement

    new engine result

    model result

    portfolio result

    NinjaTrader parity result.

A wave containing only:

    reports
    taxonomy
    literature notes
    specs

is NOT sufficient research progress.


================================================================================
50. RECURSIVE HYPOTHESIS EXPANSION
================================================================================

Never casually say:

    "the hypothesis space is exhausted."

Before a major research frontier is declared low-EVI:

run at least THREE independent expansion passes from materially different
conceptual domains.

Example:

PASS 1
    auction / market structure.

PASS 2
    signal processing / state estimation.

PASS 3
    cross-market / session structure.

Then optionally:

PASS 4
    machine learning / nonlinear interactions.

PASS 5
    portfolio downside engineering.

Deduplicate every proposed idea against:

    tested configs
    rejected ideas
    prior reports.

Specific families may be exhausted.

The GLOBAL universe must not be declared exhausted because 10 indicators
failed.


================================================================================
51. AUTOMATIC NEXT-WAVE LOGIC
================================================================================

Do not ask the owner after a wave.

Automatically continue.

IF current DAY-ONLY master parity is incomplete:

    finish parity before changing its alpha.

IF OneContract confirmation passes:

    build next NinjaScript version
    and run parity.

IF it fails:

    kill exact family
    and launch ONE new bounded discrete-policy family.

IF Engine #3 passes:

    combine on a coarse family-risk grid.

IF Engine #3 fails:

    launch the next independent Engine #3 family.

IF first Engine #3 slate fails:

    run three new mechanism-expansion passes.

IF viability state is monotonic:

    test simple 0 / 0.5 / 1 exposure.

IF variance ratio / Kalman / entropy adds no incremental information:

    kill that feature family.

IF simple ML improves fixed rules:

    test trees.

IF trees do not robustly beat simple ML:

    stop before DL.

IF DL adds material robust deployable value:

    run seed stability
    model ablation
    inference parity.

IF DL fails:

    kill it.

IF a new champion appears:

    aggressively red-team it.

IF challenger fails:

    retain incumbent
    and move to a new mechanism.

DO NOT:

    keep modifying the same failed candidate forever.


================================================================================
52. IMMEDIATE EXECUTION PRIORITY
================================================================================

At current local HEAD, FIRST determine which of these remain unfinished.

Then run remaining tasks IN PARALLEL where independent.

P0 — IMPLEMENTATION TRUTH

    current repo reconciliation

    consolidated DAYONLY master

    Python ↔ NT8 parity

    true BAR-MTM

    vol-match causality

    holiday/early-close handling.

P1 — ONE CONTRACT

    frozen challenger confirmation

    DD attribution

    new discrete policy only after confirmation result.

P2 — DRAWDOWN

    current portfolio top-DD decomposition

    CDaR / EDaR / Ulcer frontier

    short-risk crisis red team

    winner-drought diagnostics

    common-regime attribution.

P3 — CAUSAL STATE

    opportunity/friction viability

    variance ratio

    path efficiency

    Kalman innovation quality

    entropy/chop state

    HTF generalization.

P4 — ENGINE #3

    failed range/value reacceptance

    failed ORB

    small-gap fade

    VWAP structural transition

    inventory liquidation

    post-shock normalization

    ES/NQ relative state.

P5 — MODELING

    interpretable engine-quality models

    simple router

    tree models only after simple evidence

    sequence models only after tree evidence.

P6 — PORTFOLIO

    broad family weights

    downside-optimal frontier

    Engine #3 integration if qualified

    causal router.

Do NOT wait for P0 to finish before independent P3/P4 research if the
underlying research data are unaffected.


================================================================================
53. PRACTICAL NINJATRADER REQUIREMENT
================================================================================

Every final practical product must be:

    deterministic

    Strategy Analyzer reproducible

    one consolidated target process

    flat before verified margin deadline

    realistic commission

    realistic slippage

    no competing account strategies

    no hidden state that Python has but NinjaTrader cannot reproduce

    no future/session information unavailable at decision time.

Effective parameters must be logged.

Research configuration must default to realtime-disabled.


================================================================================
54. CAPITAL / MARGIN REALITY
================================================================================

Do not confuse:

    intraday broker margin

with:

    economically safe risk capital.

For each final NQ/MNQ product calculate:

    broker margin requirement
    historical DD
    stressed DD
    capital required for DD ratios
    gap stress
    expected shortfall
    margin-cliff exposure.

The amount needed to avoid liquidation is NOT:

    appropriate strategy capital.


================================================================================
55. GIT / RESEARCH DISCIPLINE
================================================================================

Do not use:

    git add .

Stage curated files only.

Do not commit:

    raw large market datasets
    temporary caches
    credentials
    protected vendor binaries
    giant model caches
    uncompressed temporary outputs.

Every major completed task writes a concise handoff containing:

    Git state
    task scope
    files changed
    validation
    result
    explicit non-runs
    caveats
    next action.

Successful validated work:

    commit and push.

Failed alpha hypothesis:

    preserve result/report/registry evidence as appropriate.

Failed/broken implementation:

    do not push broken production-like source.

No force-push.

No history rewrite.


================================================================================
56. HONEST LANGUAGE
================================================================================

Use:

    current best discovered candidate

    current-regime historical evidence

    mechanically parity-supported

    broad plateau

    causal historical result

    research champion

    challenger.

Avoid unsupported:

    optimal globally

    guaranteed

    proven future profit

    OOS-proven

    safe to leverage

    production ready

    structurally permanent.

Every important conclusion receives one label:

    FACT

    INFERENCE

    HYPOTHESIS

    EXTERNAL PRIOR.

If a previous claim is wrong:

    correct it openly.

Never protect previous conclusions.


================================================================================
57. FINAL SYSTEM PACKAGE
================================================================================

Eventually produce:

A. FULL DAY-ONLY PORTFOLIO MASTER

    NinjaScript source
    exact engine logic
    exact parameters
    exact scaling
    exact flatten logic
    parity
    cost stress.

B. ONE-CONTRACT SYSTEM

    1 NQ
    1 MNQ
    capital maps
    parity
    limitations.

C. SIMPLE ROBUST SYSTEM

    minimal architecture retaining ~90–95% robust benefit.

D. ENGINE CARDS

    Solar
    HTF
    B-MOM
    Engine #3+
    viability state
    models if promoted.

E. PORTFOLIO PARETO FRONTIER

    Sharpe
    Calmar
    DD
    CDaR
    EDaR
    TUW
    smoothness
    growth.

F. MODEL ARTIFACTS

    frozen preprocessing
    feature schema
    model
    training commit
    model card
    OOF predictions
    inference parity.

G. RISK REPORT

    native
    equal-vol
    equal-exposure
    equal-capital
    true MTM
    stress
    capital requirement.

H. ECONOMIC EXPLANATION

    why profits occur
    why losses occur
    regime dependency
    invalidation conditions.

I. HONESTY LABEL

    historical current-regime system
    not guaranteed
    not pristine-OOS proven
    no live activation
    no leverage recommendation.


================================================================================
58. FINAL REQUIRED ANSWERS
================================================================================

Before declaring the program mature, answer:

1. What is the best DAY-ONLY graded system?

2. What is the best exactly-one-contract strategy?

3. What is the best one-NQ version?

4. What is the best one-MNQ version?

5. What is the simplest system retaining 90–95% of the robust benefit?

6. Which improvements are actual alpha?

7. Which are diversification?

8. Which are exposure/risk transformations?

9. Why did original Solar draw down so deeply?

10. Why does the current champion still draw down?

11. How much DD was removed by:

       graded sizing
       HTF
       conditional short reduction
       B-MOM
       B1 removal
       Engine #3
       routing/state?

12. Is the current portfolio DD real at executable BAR-MTM level?

13. Can DD be reduced further without killing the right tail?

14. Why does exactly-one-contract lose graded-portfolio benefits?

15. Can 1 NQ achieve an economically acceptable DD?

16. Is 1 MNQ the honest lower-capital one-contract solution?

17. Does a causal edge-viability state exist?

18. Does long-horizon trend context genuinely add conditional information?

19. Does MA crossover help, or only add lag?

20. Which trend-quality feature adds the most incremental information?

21. Does Engine #3 earn when Solar/B-MOM lose?

22. Are Solar and B-MOM merely two expressions of the same regime factor?

23. Did any ML model add robust after-cost value?

24. Did tree models beat interpretable models?

25. Did deep learning add enough value to justify itself?

26. Does a router beat a fixed portfolio?

27. Does the system survive realistic execution stress?

28. What happens when engine correlations rise toward one?

29. What happens during winner drought?

30. What is the worst causal regime for the system?

31. What observable state would indicate the edge is economically weak?

32. Which strategy component is responsible for remaining tail risk?

33. What historical capital is required for each product?

34. What is the exact NinjaTrader implementation?

35. Which assumptions remain weakest?

36. What evidence would falsify the current system thesis?


================================================================================
59. STOP CONDITION
================================================================================

Do NOT stop because:

    one champion exists

    one improvement exists

    one family fails

    DD falls below a chosen dollar threshold

    Sharpe exceeds 1.2

    a document says FINAL

    a long report was produced.

A SPECIFIC FAMILY may stop when:

    repeated bounded tests fail

    mechanism contradicted

    no plateau

    economics fail realistic costs

    EVI is low

    red team agrees.

The GLOBAL PROGRAM may stop only when:

1. current practical master is implemented and parity-tested;

2. one-contract frontier is resolved;

3. current drawdown has a causal decomposition;

4. multiple independent mechanism-generation passes have completed;

5. high-EVI current-regime families are tested or formally deferred;

6. a serious independent complementary-engine search has been conducted;

7. ML has been tested to the appropriate complexity;

8. DL has either earned escalation or been honestly stopped;

9. current champion survives:
       costs
       chronology
       regime
       DD
       right-tail
       correlation
       execution
       parity
       red-team stress;

10. a final Pareto frontier exists;

11. additional testing is more likely to increase selection bias than useful
    information.

Even then NEVER claim:

    global mathematical optimality.

The correct conclusion is:

    BEST SYSTEM DISCOVERED UNDER THE CURRENT HISTORICAL RESEARCH FRONTIER.


================================================================================
60. CONTINUOUS AUTONOMOUS LOOP
================================================================================

Repeat:

    READ CURRENT FRONTIER

        ↓

    IDENTIFY HIGHEST-EVI WEAKNESS

        ↓

    SEARCH FOR DISTINCT MECHANISMS

        ↓

    DEDUPLICATE AGAINST PRIOR WORK

        ↓

    FREEZE BOUNDED TEST

        ↓

    RUN REAL AFTER-COST ANALYSIS

        ↓

    TRUE MTM / RISK / RIGHT-TAIL ANALYSIS

        ↓

    PROMOTE OR KILL

        ↓

    IF PROMOTED:
        IMPLEMENT / PARITY / PORTFOLIO TEST

    IF KILLED:
        MOVE TO NEXT INDEPENDENT MECHANISM

        ↓

    RED TEAM

        ↓

    UPDATE:

        CURRENT_TRUTH
        SYSTEM_SCORECARD
        CLAIM_LEDGER
        NEXT_RESEARCH_QUEUE

        ↓

    CONTINUE.

Do not ask the owner whether to continue.

Do not summarize and stop.

Do not force a better answer.

Do not manufacture an improvement.

Keep researching until the frontier itself says remaining expected value of
information is low.


================================================================================
61. START NOW
================================================================================

Immediately:

1. inspect CURRENT LOCAL HEAD;

2. determine what work from this directive is already complete;

3. do NOT rerun completed research;

4. launch remaining independent tracks in parallel;

5. prioritize:

       true executable master
       one-contract confirmation
       true MTM DD
       remaining-DD attribution
       causal viability state
       Engine #3
       portfolio downside frontier
       trend-quality states
       ML only where justified;

6. continuously expand the hypothesis frontier through /deep-research and
   empirical branching;

7. keep all practical candidates NinjaTrader-compatible;

8. never wait for future dates or simulation;

9. never overclaim;

10. never stop merely because previous research called something FINAL.


THE CENTRAL MISSION IS:

    TAKE THE REAL SOLAR EDGE,

    KEEP ITS VALUABLE RIGHT TAIL,

    IMPROVE ITS STATE-DEPENDENT RISK,

    COMBINE IT WITH REAL INDEPENDENT ALPHA,

    REDUCE MULTI-MONTH BLEED,

    REDUCE TRUE MARK-TO-MARKET DRAWDOWN,

    BUILD A HIGHER-SHARPE / HIGHER-CALMAR PORTFOLIO,

    BUILD THE BEST HONEST EXACTLY-ONE-CONTRACT ALTERNATIVE,

    AND IMPLEMENT THE RESULT EXACTLY IN NINJATRADER.


PUSH HARD.

THINK FROM FIRST PRINCIPLES.

SEARCH BROADLY.

TEST NARROWLY.

FALSIFY AGGRESSIVELY.

PRESERVE NEGATIVE RESULTS.

KEEP THE INCUMBENT WHEN CHALLENGERS FAIL.

CHALLENGE IT AGAIN WITH A DIFFERENT MECHANISM.

DO NOT WAIT.

DO NOT HALLUCINATE.

DO NOT MANUFACTURE A WIN.

KEEP GOING.


================================================================================
END OWNER MASTER DIRECTIVE V4
================================================================================
