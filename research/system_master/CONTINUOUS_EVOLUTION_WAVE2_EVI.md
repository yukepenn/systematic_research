# CONTINUOUS SYSTEM EVOLUTION — Wave 2 EVI recomputation

**Date:** 2026-08-09, per owner's MASTER DIRECTIVE v2 (sec19: "RECOMPUTE EVI ONCE before Wave 2
begins... do not blindly accept [the prior] ranking"). Repo state verified clean at `13cdad0`
before this recompute (`git status`/`git log -5`/`git branch` all confirmed). `LOCKED_FORWARD.md`
re-read fresh: everything through 2026-07-31 is research-consumed; ≥2026-08-01 remains sealed
except quarterly MONITOR-01 readings on the separately-frozen R5-E10 champion — unaffected by
anything in this doc, no virgin data is touched below.

## Wave-1 closed-family map (verified against the actual files, not reconstructed from prompt text)

| Family | Actual status (from its own REPORT.md/VERDICT.md) | NOT-YET-TESTED residual |
|---|---|---|
| U1 | CLOSED, mixed H0/H1 — Product-B entry ETH>RTH (moderate); continuation RTH>ETH cross-product-corroborated (strongest finding) | Hierarchical ETH-hold delta (session-only, no 2nd dimension) |
| U3 | CLOSED, fails right-tail — overnight-hold weakness real+stable but 9/20 top winners profitable while overnight | 2-D session×quality refinement (session alone insufficient) |
| U4 | CLOSED, partial — giveback@first-M-decay cleanest tail result in campaign history, degrades at n=40, A never clears | superseded — U4B tested and closed it (see below) |
| U4B | **NOT PROMOTED** — signal-gated short flatten loses net+DD at all 3 grid cells | requires genuinely NEW state variable to reopen (sec43) — none proposed yet |
| U5 | CLOSED, NOT SUPPORTED — vwap_disp_atr was a sunk-profit confound, caught pre-construction | none — any future soft-weighting feature must pass U5's forward-only test first (sec44) |
| U6 | CLOSED, not tail-safe — scale-in premium mostly \|M\| conviction; low-exposure trajectories distinguishable but both tails start there | Scale-RATE (not filter) responsiveness — explicitly reframed by directive sec27 as a policy-layer (not new-indicator) hypothesis |
| U7 | CLOSED, advisory — 2026 anomaly = regime loss-severity rise tracking volatility, analogs only ~30% generalize | Reopening requires session/organization/liquidity/microstructure, NOT another delay construction (sec42) |
| U2 | CLOSED (old question) — tick/BBO chronology-incompatible for LOYO-style Track-L testing | Directive sec33 reframes this as a DIFFERENT, NEW question (Track-C prequential viability) — not a reopening, a new track |

No family is being reopened under a new name; every family below is either explicitly authorized
by the directive as a *distinct* mechanism/track or targets a residual the closed report itself
flagged as unexplored.

## EVI scoring (mechanistic novelty / info potential / future-current relevance / prequential
testability / cross-product support / DD potential / right-tail safety potential / sample
quality / feasibility / DoF / redundancy)

| Family | Mechanism | Track | Novelty | Future-relevance | Right-tail risk | DoF | EVI |
|---|---|---|---|---|---|---|---|
| **U1B** — session-conditioned HOLD policy (ETH delta on exit/exposure while holding) | Builds directly on U1's strongest, cross-product-corroborated finding | L | Low (uses known state) but POLICY-layer is new | High — persistent structural pattern, not a 2026 artifact | Must explicitly avoid U3's failure mode (binary overnight flatten) — mitigated by using a graded delta, not a hard exit | 1 mechanism, 2-3 grid cells | **Highest** |
| **U6B** — Product-A scale-RATE conditioned on state quality | Reframes U6's already-validated quality features (sigma460/htf_agree/vote_dispersion) as a transition-speed multiplier, not an entry filter | L | Low-medium (known features, new policy mechanism) | High — targets Product A's own structural behavior, not a recency artifact | Naturally right-tail-safer than a filter (never blocks/shrinks the target, only paces arrival) | 1 mechanism, small grid | **High** |
| **U8** — path organization/entropy | Genuinely new feature CLASS (permutation entropy, sign-transition entropy, run-length persistence) vs U0's existing trend/range efficiency | L | High IF non-redundant (must be checked first, sec31) | Medium — no reason to expect regime-dependence a priori | Diagnostic only this wave — no construction until residual info is proven | small preregistered feature set | **High, gated on redundancy check** |
| **U9** — true microstructure provenance + prequential infrastructure | Reopens U2 as a NEW track (Track-C prequential viability, not Track-L LOYO) per directive sec33-39 | C | High (genuinely new data class) | Highest in principle (owner's own future-readiness priority) — but gated entirely on provenance | Cannot be assessed until provenance proven; large infrastructure cost before any alpha signal | infrastructure-only this wave | **High potential, high cost — infrastructure gate first** |
| U3B — session × quality 2-D | Needs a fresh preregistration; ONE quality dimension only (CLV_aligned or M_abs, per U3/R4 precedent) | L | Low-medium | High | Untested — this is exactly what stops it going first | more DoF than U1B/U6B (2 dimensions) | Lower than U1B this wave — deferred |

## Decision

Run **U1B, U6B, U8, U9(provenance+infrastructure only)** in parallel this wave — the exact split
directive sec72 itself suggests. U3B is deferred (lower EVI given it needs a full 2-D
preregistration from a blank slate, while U1B/U6B have concrete, already-validated single-state
mechanisms ready to construct). U9 is scoped to provenance-proving and prequential-framework
design ONLY this wave (directive sec34's gate must pass before any alpha feature is tested) — not
a promise of an eventual overlay, an honest infrastructure investment.
