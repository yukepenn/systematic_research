# INDICATOR_FEATURE_FRONTIER — Conditioning Axes: Verdicts

_2026-08-08. Every feature/conditioning axis evaluated for INCREMENTAL information
conditional on Solar (BASE vs BASE+X protocol, matched exposure, right-tail gates)._

## PASSED

- **HTF trend agreement (daily SMA50/200 state × vote sign, ×1.25 up-weight-only):
  SM08 PASS all gates, both lengths** — ΔSharpe +0.17/+0.18 (P≤0.0005), maxDD −13.6%,
  positive all 5 dev years, FLIPS 2006-2021 to positive, implementable form survives.
  → SOLAR_HTF_TILT candidate. Directive Q8 YES / Q9 YES-as-tilt (MA adds information
  as exposure shaping, NOT as a lagging entry gate — the gate form stays dead). FACT

## REAL SIGNAL, NOT MONETIZABLE (as tested)

- Consensus |vote_pend| (SM02B: hit rate 0.383→0.407 monotone; 10-13 bucket = 13.3%
  of trades, 35.9% of net, 39.0% of top-1%): map-shape arms U1/U3 improve all point
  estimates, tail-ALIGNED (retention >1), but P(dSharpe)=0.137 best → SM04 FAIL.
  The linear E10 map is near-optimal for its risk. FACT
- Vol-surprise lag-U (C01): CI-real, post-2024-only → REJECTED (revival needs mechanism).
- AUC-0.56 trade-quality state (C01 ML): real, all suppression channels tail-adverse.

## FALSIFIED / EMPTY

- Morning trend-day classifiers (gap size, IB narrowness, value-exit): SM07 —
  phase-1 precision lift 1.2× at grid edge; dev P=0.19 best. DEAD.
- σ-percentile filters: NON-monotone (mid-σ worst, −$0.28M; 59.8% of top-1% enters in
  the TOP σ quintile — never down-weight high σ; naive vol targeting is tail-adverse,
  ARM_C precedent). FACT
- Flip density / disagreement / false-start clustering as DD predictors: flat inside
  every DD episode (SM02 atlas). DEAD for drawdown control.
- Loss streaks (trade and day level): no negative conditional expectancy — reactive
  throttles anti-edge. DEAD.
- Day-scale compression→expansion (SM10), announcement conditioning (C01 both
  directions), short-side gating (SOLAR-01), threshold engineering as a class,
  chop vetoes, wave-index, Type-2/3 sleeves: all inherited DEAD.

## OPEN (bounded, unfunded — low EVI after this program's passes)

- Kalman slope/uncertainty tilt (DR_SM_C EVI-4): same mechanism family as the passed
  HTF tilt; marginal EVI now that the tilt holds the slot; only worth testing against
  TILT50 as incremental (BASE+X where BASE already includes the tilt).
- Sequential CUSUM/GLR on sleeve P&L (decay detection): monitoring tool, not alpha —
  belongs in MONITOR-01 v2.
- Session-bucket structure (15:00-17:00 best/overnight worst): analysis lens only;
  any suppression form is blocked by the right-tail gate arithmetic.
