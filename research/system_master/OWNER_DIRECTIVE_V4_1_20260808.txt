Received 2026-08-08, after the V4 opening wave close-out (HEAD bb5ca00).
Owner postscript (verbatim): "And if anything really fails it's performance at 2026.. that's
not really want. we really want a system that earns money in the future not only in the past."

================================================================================
OWNER CONTINUATION ORDER V4.1
NO WAVE-BOUNDARY STOP
SOLAR-CORE CHALLENGE
SMOOTHNESS / JOINT-LOSS RESEARCH
================================================================================

Continue from CURRENT LOCAL HEAD.

The last V4 opening wave was useful and accepted.

However:

    A WAVE CLOSE-OUT IS NOT A STOP CONDITION.

You ended after writing:

    "The V4 opening wave is complete"

while NEXT_RESEARCH_QUEUE still contains multiple high-EVI tasks.

That is an administrative boundary, not a scientific stopping point.

From now on:

    when one wave finishes,
        update CURRENT_TRUTH,
        update SCORECARD,
        update CLAIM_LEDGER,
        freeze the next bounded wave,
        commit the specs,
        AND LAUNCH IT IMMEDIATELY.

Do not return to the owner merely because a wave completed.

Within available runtime/context/tool limits, chain successive waves
automatically.

If a hard runtime/context/tool limit genuinely prevents another wave:

    write NEXT_HANDOFF,
    preserve exact continuation state,
    and resume from it on the next invocation.

Do NOT ask the owner which item to run next.

Use expected value of information.

================================================================================
1. ACCEPT THE LATEST MECHANICAL TRUTH
================================================================================

Verify against local HEAD first.

Expected latest evidence includes:

- SolarWaveSMMaster_v2 consolidated master parity PASSED.
- executable development headline approximately:
      Net        $177k
      Sharpe     ~1.17
      MaxDD      ~-$18.9k
      CDaR       ~-$14.9k
      WorstMonth ~-$7.5k
- one-contract A-dominant confirmation FAILED;
  SM14 remains incumbent.
- 60/40 broad portfolio zone retained.
- VR and ER state families failed.
- Engine-3 slate 1 failed.
- drought tilt failed.
- C-P7 windfall give-back information pre-test survived.
- leverage add-ons currently fail the owner's DD tolerance.

Repository truth overrides these numbers if later commits differ.

Do not rerun completed work.

================================================================================
2. IMMEDIATELY LAUNCH THE NEXT EXISTING QUEUE
================================================================================

Freeze specs and run in parallel where independent:

A. C-P7 WINDFALL GIVE-BACK POLICY CONFIRMATION

    Convert the information result into a bounded causal policy.

    Do NOT assume the preliminary p-value guarantees policy value.

    Test:
        effect on Net
        Sharpe
        Calmar
        CDaR
        MaxDD
        TUW
        worst month
        right-tail retention

    Use event de-clustering.
    Honest effective N is small.
    Require placebo comparison.

B. ONE-CONTRACT FAMILY #NEXT

    The prior A-dominant policy failed mainly on confidence and
    big-winner retention.

    Test ONE genuinely new bounded discrete family,
    max 3 coarse cells.

    Primary problem:
        preserve major Solar winner days
        while retaining B-MOM's useful discrete information.

    Do not modify the previous failed family indefinitely.

C. KALMAN / BOCPD JOB-1 STATE TEST

    Kalman innovation quality
    causal change-point / regime age.

    Information test first.
    No exposure rule unless incremental information exists.

D. ENGINE #3 SLATE 2

    value-area / accepted-value rotation
    multi-day balance false-break

    plus at least one structurally distinct high-EVI mechanism generated
    by /deep-research.

    Do not repeat the killed intraday sweep-fade mechanism.

E. WINNER-DROUGHT QUANTIFICATION

F. COMPONENT RISK CONTRIBUTION

    For the executable DAYONLY master:
        Solar contribution
        B-MOM contribution
        joint loss
        winner absence
        side
        HTF
        time-of-day
        common-regime contribution.

G. MORNING / SIDE / HTF CONDITIONAL ANALYSIS

H. LEVEL-1 ML TRADE/EPISODE QUALITY SCREEN

    logistic/GAM first,
    using causal DD-anatomy features.

Run these now.

Do not summarize and stop after completion.

Auto-launch the next EVI-ranked wave.

================================================================================
3. NEW MAJOR TRACK — SOLAR CORE CHALLENGE
================================================================================

A critical owner question remains unanswered:

    WHY IS THE CURRENT SOLAR CORE THE BEST SOLAR REPRESENTATION?

The answer may NOT be:

    "because it is the current baseline."

The current 13-member ensemble was chosen for robustness under parameter
uncertainty.

That does NOT establish global or even current-regime portfolio optimality.

Create:

    SOLAR_CORE_CHALLENGE

The objective is NOT to restart historical parameter mining.

The objective is to determine whether the current Solar core is
PARETO-DOMINATED by a mechanically plausible neighboring architecture.

The incumbent must re-earn its status.

================================================================================
4. SOLAR CORE CHALLENGE — CLOCK
================================================================================

Test mechanism-level neighboring clocks:

    1-minute
    3-minute incumbent
    5-minute

under comparable causal construction and cost assumptions.

Questions:

1. Which clock gives best standalone after-cost economics?
2. Which gives best Sharpe?
3. Which gives best true MTM DD?
4. Which retains the strongest right tail?
5. Which contributes most to the TOTAL portfolio?
6. Does 1m add useful timing information even if 3m remains the best core?
7. Does 5m reduce noise but miss too much early trend?

Do NOT simply choose the clock with the highest full-sample Net.

Use chronology + plateau + portfolio contribution.

================================================================================
5. MULTI-TIMEFRAME SOLAR
================================================================================

Explicitly test whether the best architecture is:

    3m primary state
        +
    1m timing/failure state

rather than:

    pure 1m
or
    pure 3m.

Potential low-dimensional uses of 1m information:

    entry timing
    re-entry timing
    pullback completion
    failed-start detection
    short-term directional conflict
    acceleration/deceleration
    temporary risk reduction.

Do NOT automatically require 1m confirmation.

A lagging confirmation that delays the Solar edge may destroy the right tail.

Measure incremental information first.

================================================================================
6. SOLAR VOLATILITY MEMORY
================================================================================

The current adaptive threshold depends on a causal volatility estimate.

Do not assume the existing memory length is optimal.

Test only BROAD mechanism scales, for example:

    shorter-memory family
    incumbent-memory family
    longer-memory family

approximately half / current / double scale where technically sensible.

Do NOT fine-grid every integer period.

For each report:

    responsiveness
    turnover
    threshold distribution
    clamp frequency
    Net
    Sharpe
    Calmar
    DD
    right-tail
    portfolio contribution.

Question:

    Is the incumbent memory in a broad robust plateau?

If yes:
    retain.

If no:
    investigate mechanism before changing it.

================================================================================
7. SOLAR ENSEMBLE RANGE / MEMBER CONTRIBUTION
================================================================================

Audit the 13-member ensemble structurally.

Do NOT select historically best individual members.

Group members into broad cohorts:

    FAST
    MID
    SLOW

and measure:

    return contribution
    variance contribution
    DD contribution
    ES contribution
    top-trend-day contribution
    churn
    disagreement
    marginal portfolio benefit.

Run bounded cohort ablations:

    remove FAST cohort
    remove SLOW cohort
    MID-only diagnostic
    incumbent all-member ensemble.

Question:

    Do all threshold scales earn their place?

If an entire broad cohort consistently adds risk without return:
    it may be removable.

Do NOT pick individual winners post hoc.

================================================================================
8. SOLAR SIGNAL-LAYER CHALLENGE
================================================================================

Use the recovered Solar semantics.

T1/T2/T3 represent different state transitions.

Do NOT assume only T1 is the only useful system-level information.

Research separately:

    T1 = trend birth / flip
    T2 = pullback opportunity
    T3 = strengthening / resumption

Possible jobs:

    T1 as core directional state
    T2 as selective re-entry / add timing
    T3 as confidence / continuation information.

Do NOT immediately combine them into one flexible rule.

First measure:

    conditional expectancy
    overlap
    timing
    MFE/MAE
    right-tail contribution
    incremental information conditional on current Solar state.

If T2/T3 add no independent information:
    kill them.

If one adds stable information:
    test a very simple policy.

================================================================================
9. SOLAR CLAMP / THRESHOLD ARCHITECTURE
================================================================================

Before tuning SMin/SMax or other bounds:

first measure how often each bound actually binds.

If a bound almost never binds:
    do not waste parameter budget on it.

If binding materially affects current-regime trades:
    test only coarse neighboring structural bounds.

No exact historical argmax.

================================================================================
10. 30/59 MA / SHORTER STATE MACHINE
================================================================================

The owner specifically asks about ideas such as 30/59 MA or other
short-term confirmation.

Test them scientifically.

Do NOT assume:

    Solar up + MA up = better.

Solar already contains directional trend information.

A lagging MA may simply enter later and destroy profitable right-tail entries.

Test MA/state information in FOUR distinct jobs:

JOB A
    hard entry confirmation.

JOB B
    soft exposure confidence.

JOB C
    short-term failure / adverse transition warning.

JOB D
    re-entry timing after temporary conflict.

Compare:

    no MA
    representative 30/59-style state
    another broad neighboring MA pair
    a non-MA trend-quality state.

The key question:

    DOES THIS ADD INCREMENTAL INFORMATION CONDITIONAL ON SOLAR?

Not:

    does the MA look good on a chart?

================================================================================
11. PRODUCT D — PORTFOLIO-OPTIMAL SOLAR
================================================================================

Create a formal frontier:

    CURRENT SOLAR CORE
    1m CORE
    5m CORE
    MULTI-TIMEFRAME CORE
    alternative broad vol memory
    cohort-ablation variants
    simple signal-layer variants.

Rank each on:

A. standalone economics

and separately:

B. TOTAL PORTFOLIO contribution when combined with frozen B-MOM.

The portfolio-optimal Solar may NOT be the standalone-max-Net Solar.

This is PRODUCT D.

Do not change the full master until a challenger passes predefined
chronology/plateau/risk gates.

================================================================================
12. SMOOTHNESS FRONTIER — FORMAL RESEARCH PROGRAM
================================================================================

The owner wants a system that is much more consistently profitable.

Do NOT interpret that as an impossible requirement that every
day/week/month must be green.

Instead find the best attainable Pareto frontier for:

    positive-day %
    positive-week %
    positive-month %
    positive-quarter %

    longest losing-day streak
    longest losing-week streak
    longest losing-month streak

    worst week
    worst month
    worst quarter

    rolling20 floor
    rolling60 floor
    rolling120 floor

    TUW
    recovery duration

while preserving:

    after-cost expectancy
    Sharpe
    Calmar
    ES
    right-tail
    robust log growth.

Compute these metrics immediately for:

    Solar E10
    SOLAR_DUAL_HTF
    B-MOM
    executable DAYONLY master
    SM14 one-contract.

The existing wave does not report enough positive-day/week/month
statistics.

Add them to SYSTEM_SCORECARD permanently.

================================================================================
13. JOINT-LOSS PERIOD ANALYSIS
================================================================================

For every week and month classify:

    Solar positive / B-MOM positive
    Solar positive / B-MOM negative
    Solar negative / B-MOM positive
    Solar negative / B-MOM negative.

Focus especially on:

    JOINT-LOSS WEEKS
    JOINT-LOSS MONTHS.

For those periods ask:

    What causal market state existed BEFORE and DURING them?

Potential explanatory axes:

    low opportunity/friction ratio
    low directional efficiency
    repeated failed trends
    high flip rate
    range-bound auction
    cross-index disagreement
    volatility regime
    event regime
    time-of-day loss clusters
    side asymmetry
    winner drought.

The purpose of Engine #3 / router is specifically to attack these periods.

Do NOT build a rule from the fact that the equity curve is currently down.

Use market state.

================================================================================
14. ENGINE #3 SHOULD TARGET JOINT-LOSS STATES
================================================================================

A third engine is strategically useful if:

    its standalone expectancy is positive

and:

    it earns or remains neutral
    during Solar+B-MOM joint-loss weeks/months.

Prioritize truly distinct mechanisms:

    balanced-value rotation
    failed persistence
    multi-day false break
    inventory correction
    VWAP/value reacceptance
    opening-auction failure
    post-shock normalization
    ES/NQ relative state
    event digestion.

Do not require Engine #3 to have the highest standalone Sharpe.

Require total-system DD / CDaR / TUW improvement.

================================================================================
15. CAN WE APPROACH "GREEN MOST WEEKS / MONTHS"?
================================================================================

Build a constrained SMOOTHNESS PARETO experiment.

Do NOT directly maximize positive-month percentage.

Instead:

CONSTRAINTS:

    positive after-cost Net
    Sharpe not materially worse than incumbent
    Calmar not materially worse
    no catastrophic left-tail deterioration
    right-tail retention acceptable
    realistic execution
    complexity bounded.

WITHIN those constraints:

    maximize positive-month %
    then positive-week %
    minimize losing-month streak
    minimize worst rolling60.

Report the actual attainable frontier.

If:

    100% positive months is impossible,

say so.

If:

    80–90% positive months is historically attainable only by
    destroying right-tail economics,

say so.

Do not manufacture a smooth backtest.

================================================================================
16. DAILY PROFITABILITY
================================================================================

Daily green percentage is secondary to weekly/monthly quality.

Why:

    a healthy right-tail trend system naturally has many small losing days.

Nevertheless report it.

Search whether independent engines can increase:

    positive-day %
    without worsening:
        payoff ratio
        skew
        ES
        maxDD.

Do not optimize daily hit rate in isolation.

================================================================================
17. REMAINING DD MUST KEEP FALLING IF REAL INFORMATION EXISTS
================================================================================

Current executable historical MaxDD is approximately -$18.9k according
to the latest known wave.

Treat it as the current executable reference only after repo verification.

Do NOT stop there.

But do NOT target "$12k" mechanically.

For every DD-reducing challenger compute:

    same-risk DD
    same-vol DD
    same-ES DD
    same-CDaR DD
    right-tail retention
    net retention
    geometric growth.

If lower DD only comes from lower exposure:
    classify it as risk reduction, not alpha.

If a causal state removes low-expectancy exposure:
    that is a real improvement.

================================================================================
18. C-P7 WINDFALL RESULT — SKEPTICAL CONFIRMATION
================================================================================

The windfall give-back information test is intriguing but low-N.

Do not overclaim p=0.0012.

De-cluster events.

Test frozen policy.

Include:

    leave-year-out
    leave-2022-out
    block bootstrap
    placebo
    right-tail retention
    net
    Sharpe
    CDaR
    TUW.

If it fails:
    kill.

If it passes:
    integrate only as a simple causal risk policy.

================================================================================
19. ML FOR SMOOTHNESS / DD
================================================================================

After simple states:

Level-1 models may target:

    P(next Solar episode loss)
    P(next Solar top-decile winner)
    P(next B-MOM trade loss)
    P(next session joint Solar+B-MOM loss)
    expected portfolio return next session
    conditional portfolio downside next 5 sessions.

Inputs must be market-state variables:

    HTF
    side
    consensus
    opportunity/friction
    time-of-day
    volatility
    flip rate
    state age
    cross-market state
    value/auction state.

Do not use:

    recent equity drawdown

as a magical predictor unless future expectancy conditional on it is
demonstrated beyond market-state controls.

Start logistic/GAM.

Only escalate if robust.

================================================================================
20. DO NOT DECLARE CURRENT SOLAR PARAMETERS "OPTIMAL"
================================================================================

Until SOLAR_CORE_CHALLENGE is complete, the correct language is:

    CURRENT ROBUST SOLAR INCUMBENT

not:

    OPTIMAL SOLAR PARAMETERS.

The incumbent can survive.

That is allowed.

But it must survive because:

    neighboring clocks fail
    neighboring memory scales fail
    cohort alternatives fail
    signal-layer alternatives fail
    lower-timeframe timing adds no robust value

—not because nobody challenged it.

================================================================================
21. AUTO-CHAIN WAVES
================================================================================

At the end of this wave:

DO NOT RETURN WITH ONLY A CLOSE-OUT.

If high-EVI work remains:

    update docs
    freeze next specs
    commit specs
    launch next wave immediately.

Continue this cycle.

Only stop a specific family under its kill rule.

Do not stop the global program because one slate fails.

Do not wait for future monitoring dates.

Do not ask owner.

================================================================================
22. EXPECTED NEXT OUTPUTS
================================================================================

Before this continuation can be considered mature, I want concrete answers
to:

1. Why exactly is the current Solar core still incumbent?
2. Is 3m really superior to 1m/5m at system level?
3. Does 1m add useful timing/failure information?
4. Is the current volatility memory a robust plateau?
5. Do all 13 member cohorts earn their place?
6. Do T2/T3 contain useful incremental information?
7. Does 30/59-style short-term state help or merely add lag?
8. What percentage of days/weeks/months are currently positive?
9. What causes negative weeks/months?
10. What causes joint Solar+B-MOM losing periods?
11. Can Engine #3 reduce those?
12. Can the executable -$18.9k DD be reduced at equal risk?
13. Can worst month materially improve from roughly -$7.5k?
14. Can TUW materially shorten?
15. Can the one-contract DD frontier improve beyond SM14?
16. Does C-P7 truly improve the system?
17. Do Kalman/BOCPD states add information?
18. What is the next independent hypothesis after Engine-3 slate 2?
19. What does the smoothness Pareto frontier actually look like?
20. What is the best current PRODUCT A / B / C / D after all of this?

Execute now.

Do not summarize and stop.

Continue into the next wave automatically.

================================================================================
END OWNER CONTINUATION ORDER V4.1
================================================================================
