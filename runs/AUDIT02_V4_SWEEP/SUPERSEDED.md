# SUPERSEDED before execution

No V4 run was ever executed under this directory's spec. The A-arm V3 sweep
falsified the README recipe's `StartUp=true` before any V4 job was launched, so the
V4 reproduction runs under `runs/AUDIT02_V4_SWEEP_B/` with `StartUp=false`.
The spec.yaml here is retained unmodified as preregistration evidence.
