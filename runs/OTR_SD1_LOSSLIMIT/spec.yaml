run_id: OTR_SD1_LOSSLIMIT
campaign: original_trader_reconstruction
phase: "Phase 4 (directive sec 13) — RKSelTimeDSTM LossLimit semantics, bounded family"
date: 2026-08-23
run_class: RECONSTRUCTION_PASS
spec_frozen_before_results: true

evidence: >
  Truncated name RKSelTimeDSTM..., core 90/179/5/10/10 qty 1 unchanged, LossLimit=2500
  (one screenshot) and 4000 (another). Era unknown ("later"); the 2025-09 era screenshot
  still shows 90/179/5/10/10 -> the late-2025 windows are the most plausible SD-era
  targets. DSTM expansion remains Class D.

semantics_implemented (directive's A-F collapsed to the 3 implementable + distinguishable):
  A_per_trade: exit when the OPEN trade's bar-close MTM <= -LossLimit (fill next open)
  B_session_realized: when realized session PnL <= -LossLimit, no NEW entries (and
    reversals degrade to plain exits) until next session; open position exits normally
  C_session_mtm: when realized + open MTM <= -LossLimit at bar close, flatten (next open)
    and disable until next session
  collapsed: D (daily kill switch) == C's flatten+disable behavior; E (strategy-level
  threshold) not adjudicable on weekly windows; F == C. Recorded in LOSSLIMIT_SEMANTICS.md.

grid_frozen:
  base: OTR-S-CAND1 (frozen Track-S candidate; no retuning)
  semantics: [A, B, C] x LossLimit in [2500, 4000]
  windows: canonical 2023-01..2025-01 (fingerprint deltas) + late-2025 segment
    (2025-08-15..2026-01-03; targets W20250921/W20251130/W20251221/W20251228)
  cells: 6 canonical + 6 late-2025
adjudication: >
  Per directive: report delta trades / hold / WR / PF / net / DD per semantic; use
  late-2025 weekly fingerprints + largest-loss discipline to rank consistency. Expected
  honest outcome: partial identification (weekly aggregates have low discrimination
  power); do NOT pick the most profitable semantic.
outputs:
  - runs/OTR_SD1_LOSSLIMIT/out/{results.json, deltas.csv}
  - research/original_trader_reconstruction/solar_dstm_family/LOSSLIMIT_SEMANTICS.md
  - HYPOTHESIS_LEDGER rows OTR-SD1-*
seed: deterministic
