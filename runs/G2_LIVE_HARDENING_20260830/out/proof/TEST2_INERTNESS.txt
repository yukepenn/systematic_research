======================================================================================================================
TEST 2 - INERTNESS AUDIT (generated by inertness_audit.py, not assembled by hand)
======================================================================================================================

### WeeklyEdgeP1PCT_v2.cs   (hardening region only)
BLOCK                                  LINE    MECH   GATE / WHY IT CANNOT RUN IN State.Historical
----------------------------------------------------------------------------------------------------------------------
LogInfo                                L239    -      Log/Print only; cannot alter a decision
LogWarn                                L242    -      Log/Print + HdAlert (HdAlert is M1)
LogErr                                 L246    -      Log/Print + HdAlert (HdAlert is M1)
HdAlert                                L250    M1     if (State != State.Realtime) return;
Halt                                   L262    M1-by-consumer writes haltEntries; haltEntries is READ ONLY inside EntriesAllowed(), which is M1
ResetShadow                            L268    -      writes shadow-ledger fields only; no certified field, never read outside M1 code
EntriesAllowed                         L275    M1     if (State != State.Realtime) return true;
NoteBlockedEntry                       L285    M1     if (State != State.Realtime) return;
OnExecutionUpdate                      L303    M1     if (State != State.Realtime) return;
OnOrderUpdate                          L324    M1     if (State != State.Realtime) return;
OnPositionUpdate                       L358    M1     if (State != State.Realtime) return;
ObserveSettlement                      L371    M1     if (State != State.Realtime) return;
AssertLedgerMatchesStrategyPosition    L398    M1     if (State != State.Realtime) return;
HdAccountPositionString                L426    M1-by-caller called only from AssertLedgerMatchesStrategyPosition (M1) inside a log string
ResolveRollDates                       L442    M1     if (State != State.Realtime || rollResolved) return;
RollBlocked                            L474    M1     if (State != State.Realtime) return false;
WarmRow                                L493    -      static string formatter
ReportWarmup                           L502    M1-by-caller called only from State.Transition / State.Realtime branches (M1) and from ReportWarmup(REARM) inside HdRealtimeBarHook (M1)
WriteWarmupCertificate                 L512    M4+empty-default returns unless WarmupCertDir != ""; default ""
HdDiagRow                              L541    M1     if (State != State.Realtime) return;
HdCloseWriters                         L566    -      closes an added StreamWriter that is null unless DiagDir set
IsMine                                 L572    -      static string predicate
HdBarTime                              L575    -      read-only Time[0] accessor in try/catch
HdBarTimeString                        L578    -      read-only Time[0] accessor in try/catch
BuildWarmupTable                       L589    M1-by-caller reads certified accumulators, writes only warmupVerdict/rows; callers are the Transition/Realtime branches and HdRealtimeBarHook (all M1)
HdEnvRows                              L610    -      read-only environment snapshot into a string list
HdLogTemplate                          L633    M1-by-caller called only from the State.Realtime branch
HdConfigAssert                         L647    M4     RUNS IN A BACKTEST (State.DataLoaded). Writes configFault + haltEntries. haltEntries is read only inside EntriesAllowed() -> M1. Cannot change a backtest trade.
TryParseWanted                         L668    -      static parser, out-params only
HdInstrumentGuard                      L680    M4     RUNS IN A BACKTEST (State.DataLoaded). P1: returns immediately (ExpectInstrument default ""). XM: CAN WRITE THE CERTIFIED FIELD instrumentMismatch -- the single non-M1 path to certified behaviour. Monotone (only ever sets true) and read only in the ENTRY predicate.
HdRealtimeBarHook                      L703    M1     if (State != State.Realtime) return;
HdSessionEndStaleCheck                 L737    M1     if (State != State.Realtime) return;
----------------------------------------------------------------------------------------------------------------------
32 methods in region; 13 gated M1 at the first executable statement; 19 justified otherwise.

### WeeklyEdgeXMConflict_v3.cs   (hardening region only)
BLOCK                                  LINE    MECH   GATE / WHY IT CANNOT RUN IN State.Historical
----------------------------------------------------------------------------------------------------------------------
LogInfo                                L206    -      Log/Print only; cannot alter a decision
LogWarn                                L209    -      Log/Print + HdAlert (HdAlert is M1)
LogErr                                 L213    -      Log/Print + HdAlert (HdAlert is M1)
HdAlert                                L217    M1     if (State != State.Realtime) return;
Halt                                   L229    M1-by-consumer writes haltEntries; haltEntries is READ ONLY inside EntriesAllowed(), which is M1
ResetShadow                            L235    -      writes shadow-ledger fields only; no certified field, never read outside M1 code
EntriesAllowed                         L242    M1     if (State != State.Realtime) return true;
NoteBlockedEntry                       L252    M1     if (State != State.Realtime) return;
OnExecutionUpdate                      L270    M1     if (State != State.Realtime) return;
OnOrderUpdate                          L291    M1     if (State != State.Realtime) return;
OnPositionUpdate                       L325    M1     if (State != State.Realtime) return;
ObserveSettlement                      L338    M1     if (State != State.Realtime) return;
AssertLedgerMatchesStrategyPosition    L365    M1     if (State != State.Realtime) return;
HdAccountPositionString                L393    M1-by-caller called only from AssertLedgerMatchesStrategyPosition (M1) inside a log string
ResolveRollDates                       L409    M1     if (State != State.Realtime || rollResolved) return;
RollBlocked                            L441    M1     if (State != State.Realtime) return false;
WarmRow                                L460    -      static string formatter
ReportWarmup                           L469    M1-by-caller called only from State.Transition / State.Realtime branches (M1) and from ReportWarmup(REARM) inside HdRealtimeBarHook (M1)
WriteWarmupCertificate                 L479    M4+empty-default returns unless WarmupCertDir != ""; default ""
HdDiagRow                              L508    M1     if (State != State.Realtime) return;
HdCloseWriters                         L533    -      closes an added StreamWriter that is null unless DiagDir set
IsMine                                 L539    -      static string predicate
HdBarTime                              L542    -      read-only Time[0] accessor in try/catch
HdBarTimeString                        L545    -      read-only Time[0] accessor in try/catch
BuildWarmupTable                       L555    M1-by-caller reads certified accumulators, writes only warmupVerdict/rows; callers are the Transition/Realtime branches and HdRealtimeBarHook (all M1)
HdEnvRows                              L570    -      read-only environment snapshot into a string list
HdLogTemplate                          L600    M1-by-caller called only from the State.Realtime branch
HdConfigAssert                         L612    M4     RUNS IN A BACKTEST (State.DataLoaded). Writes configFault + haltEntries. haltEntries is read only inside EntriesAllowed() -> M1. Cannot change a backtest trade.
TryParseWanted                         L655    -      static parser, out-params only
HdInstrumentGuard                      L667    M4     RUNS IN A BACKTEST (State.DataLoaded). P1: returns immediately (ExpectInstrument default ""). XM: CAN WRITE THE CERTIFIED FIELD instrumentMismatch -- the single non-M1 path to certified behaviour. Monotone (only ever sets true) and read only in the ENTRY predicate.
HdDeadSeriesObserver                   L728    M1     if (State != State.Realtime) return;
HdRealtimeBarHook                      L757    M1     if (State != State.Realtime) return;
HdXmAgeRow                             L787    M1     if (State != State.Realtime) return;
HdSessionEndStaleCheck                 L810    M1     if (State != State.Realtime) return;
----------------------------------------------------------------------------------------------------------------------
34 methods in region; 15 gated M1 at the first executable statement; 19 justified otherwise.

### STRUCTURAL CLAIMS (checked mechanically)
----------------------------------------------------------------------------------------------------------------------
C1 WeeklyEdgeP1PC: every read of haltEntries                     L264=LATCH-GUARD; L278=BLOCKING; L852=LOG- PASS
C1 WeeklyEdgeP1PC: every read of warmupBlocked                   L279=BLOCKING; L708=REARM                  PASS
C1 WeeklyEdgeP1PC: every read of entriesBlockedUntilAgree        L280=BLOCKING; L416=REARM                  PASS
C2 WeeklyEdgeP1PC: gate wraps only entry order sites             wrap sites at lines [1164]                 PASS
C3 WeeklyEdgeP1PC: added order sites are M1-gated                3 order sites total, 0 inside an M1 method PASS
C4 WeeklyEdgeP1PC: NO exit is behind EntriesAllowed()            gated exit sites: none                     PASS
C1 WeeklyEdgeXMCo: every read of haltEntries                     L231=LATCH-GUARD; L245=BLOCKING; L942=LOG- PASS
C1 WeeklyEdgeXMCo: every read of warmupBlocked                   L246=BLOCKING; L762=REARM                  PASS
C1 WeeklyEdgeXMCo: every read of entriesBlockedUntilAgree        L247=BLOCKING; L383=REARM                  PASS
C2 WeeklyEdgeXMCo: gate wraps only entry order sites             wrap sites at lines [1080]                 PASS
C3 WeeklyEdgeXMCo: added order sites are M1-gated                4 order sites total, 1 inside an M1 method PASS
C4 WeeklyEdgeXMCo: NO exit is behind EntriesAllowed()            gated exit sites: none                     PASS
----------------------------------------------------------------------------------------------------------------------
TEST 2 VERDICT: PASS
