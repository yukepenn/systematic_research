run_id: G2_WORLDSCAN_W1_20260828
campaign: GENESIS_II
type: WORLD_DISCOVERY_CENSUS   # no alpha claim, no P&L, no gate
spec: >
  16 disjoint source-domain scouts (academic, video, social, forums, vendors, code, old-school,
  scalping/order-flow, profile, VWAP/MR, breakout, event, cross-market, ML/representation,
  repo archaeologist, data-asset hunter) -> B1 dedup into mechanism cards + family tree + source
  graph -> B2 adversarial skeptic (kill/triage, TOP-8 EVI, Formal Wave 1 pick of 3-6).
  Workflow wf_f37e445c-70a (interrupted twice: session limit, then a B1 server error; B1/B2
  completed as standalone file-based agents).
prereg_note: census run; workflow script launched pre-results, committed with results (disclosed).
evidence: partial_snapshot/ (16 scout reports + journal + script), out/mechanism_cards.md (37
  cards, ~230 leads), out/skeptic_verdicts.md (37 verdicts + Formal Wave 1 pick).
constraints_held: public browsing only; no logins/purchases/downloads/piracy; web content treated
  as data; repo read-only for scouts; no crosstrade; $0.
live_enabled: NO
