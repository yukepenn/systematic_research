run_id: G2_F8_DUO_20260829
campaign: GENESIS_II_FORMAL_WAVE_8
type: SIGNAL_DIAGNOSTIC_DUO
preregistration_source: >
  The frozen primaries for both sub-runs are the card texts + skeptic tightenings ALREADY
  COMMITTED pre-result at 9e80f15: runs/G2_WORLDSCAN_W2_20260829/out/mechanism_cards_w2.md
  (sections MC-43, MC-53) and out/skeptic_verdicts_w2.md (same sections). Each sub-run MUST
  transcribe its frozen legs verbatim into its spec_resolutions BEFORE any computation; the
  transcription IS the executable spec; deviations = DEFECT.
sub_runs:
  A_MC43_dc_audit (trial G00037):
    role: METHODS - opens or closes the EVENTTIME family (gates MC-44/45 sequencing)
    frozen: the card's three-leg DC intrinsic-time audit on NQ 1-min with the skeptic's
      tightenings (the conditional leg runs against its MATCHED control; the analytic Brownian
      null is the partial self-control; no pristine exposure - NQ 1-min only)
    verdict_semantics: laws-hold-no-lift => EVENTTIME family closed cheaply (the likeliest and
      perfectly acceptable outcome); conditional lift above control+null => family opens
  B_MC53_regime_restricted (trial G00038):
    role: cross-asset regime states for SIZING
    frozen: the card's legs with the skeptic's restriction - DISCOVERY ON 2006-2018 ONLY
      (date_max at load on the multi-market panel; the 2019+ pristine window is NEVER loaded;
      the stock-bond-correlation modern-evidence caveat is printed with any result)
    verdict_semantics: any survivor is DISCOVERY-grade with its 2019+ confirmation explicitly
      RESERVED (a future one-shot, not this run)
shared: seal_guard on every load; POINTS/normalized units; MDEs before verdicts; program-printed
  gate tables; no parameter search; no policies; no sealed reads
outputs: outA/ outB/ per sub-run (gate_table, spec_resolutions, ledger_result_pending fragment)
live_enabled: NO
spend: $0
