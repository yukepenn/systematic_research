# G2_F8_DUO — RESULT: **EVENTTIME family CLOSED · cross-asset regime legs 0/4**

Spec committed `ad32736` pre-result; frozen legs transcribed verbatim from the pre-committed
cards (`9e80f15`) before computation. Trials G00037 (NULL) / G00038 (FAIL). Program-printed
gates in `outA/ outB/`.

## A — DC intrinsic-time audit (MC-43): **EVENTTIME-CLOSED, no lift**

Overshoot law fails only where discretization+fat-tails predict it; the 0.632 memoryless law
fails BELOW the boundary; the preregistered conditional adds nothing over its matched control
and label-shift null (β inside the null central-95, 3.6× under MDE on the paired contrast).

> ⭐ **The decisive structural fact (banked):** the circular-shift null reproduces the observed
> event-time moments almost exactly at every threshold — **NQ's departure from GBM in event time
> is carried by the fat-tailed within-session return distribution, not by path ordering.**
> Event-time/DC representations therefore add ~nothing over the marginal on this substrate.
> MC-44/MC-45 stay gated shut; duration-timing is closed cheaply, as the wave spec anticipated.

One real unconditional deviation recorded (DC-share persistently below 0.632, declining hazard)
— N-robust, unconditional, not tradable as tested; it may only re-enter via a new card.

## B — restricted cross-asset regime (MC-53): **FAIL 0/4 vs the RV-only control**

Mahalanobis turbulence, 2-state HMM, composite z-state, 10-month-MA trend — none beats the
MC-51-frozen plain-RV throttle at matched exposure (best leg 3.6× under MDE; family K_eff 2.58
bar also unmet; the turbulence leg even worsens tail). **The 2019+ pristine panel was never
loaded** (date_max inside the read, max loaded date printed 2018-12-31); the stock-bond-corr
leg's 2022+-only evidence caveat printed; no confirmation claim arises.

**Closure scopes:** *the card's four regime constructions as sizing states vs an RV throttle,
2009–2018.* Regime sophistication above plain realized vol has now failed twice at matched
exposure (VOLSIZE01, this run).

**`LIVE ENABLED = NO` · $0 · DISCOVERY_CONSUMED.**
