run_id: G2_AUG_INCUMBENT_READ_20260830
campaign: GENESIS_II
type: LOCKED_FORWARD_READ   # ONE-WAY DOOR. Owner-authorized seal read, scoped.
owner_authorization: >
  Owner instruction 2026-08-30: 「给你可以看8月的权利。不知道暂时封存有任何意义」 — explicit,
  recorded, after the cost was stated. Supersedes the ≥2026-08-01 seal FOR THE SCOPE BELOW ONLY.
scope_deliberately_narrow:
  READ: P1/PCT and XM_CONFLICT_v2 performance on 2026-08 sessions (the incumbent's first
    out-of-sample month). Rationale for allowing it: for the INCUMBENT, August is largely
    REDUNDANT with the prospective shadow that begins 2026-09-01, so the scientific loss is small.
  NOT READ (seal preserved for these): FOLLOW_MORNING's forward read (it is NOT on the shadow
    roster - August is its only accumulation原料); LIQREV01/HTFDIR01 shadow readings (2026-11-01);
    any future candidate's confirmation window. No new object may be designed against August.
burn_declaration: >
  On completion, 2026-08 is tagged DIRECTLY_BURNED for P1/PCT and XM_CONFLICT_v2. It can never
  again be quoted as clean forward evidence for those objects. The seal boundary for all other
  objects moves to ≥2026-09-01 (which is also SHADOW_START).
method_frozen:
  engine: NT8 Strategy Analyzer via CrossTrade RunStrategyBacktest (the CERTIFIED .cs objects,
    not a Python re-implementation) — WeeklyEdgeP1PCT_v1 and WeeklyEdgeXMConflict_v2, NQ 09-26,
    1-minute, template commission "NinjaTrader Brokerage Lifetime", zero slippage.
  warmup: the run starts 2026-01-01 so the strategies' rolling state (P1 460-bar vol / 250-entry
    quality window / 14-day B-MOM band; XM 60-session sigma) is filled BEFORE August. Only the
    AUGUST portion is the read; the Jan-Jul portion is used as a PARITY CROSS-CHECK against the
    Python numbers already computed (a bonus validation, not a new claim).
  session boundary: `to` = one second before the next 18:00 ET open (CLAUDE.md §6).
  cost note printed with every figure: NT8 convention = template commission + ZERO modeled
    spread; research convention adds $20.65 (P1) / $12.50 (XM) per ctrRT. The two are NOT the
    same quantity and may not be compared without the adjustment stated.
reported: August net per leg and combined (M_11), trade counts, per-week table, and the
  Jan-Jul parity delta vs the Python stream. NO gate, NO promotion, NO policy change follows.
prohibitions: no re-tuning, no new object designed on August data, no FOLLOW_MORNING computation,
  no ≥2026-09-01 read (that belongs to the shadow).
live_enabled: NO
spend: $0
