family: SPEC01_SESSION_LEAK_AUDIT
directive: "CONTINUOUS SYSTEM EVOLUTION -- Master Directive v3 / Wave 4+, sec12-13 (P0)"
frozen_before_results: true
date: 2026-08-09
information_class: SPECIFICATION_DEFECT

hypothesis: >
  The owner recalls an unresolved issue, approximately described as "~16 evening trades,
  roughly -$33.5k, very large loss per trade, material share of baseline net." Per directive
  sec3/sec12, do not accept these numbers as fact -- reconstruct the actual artifact and
  determine whether this is a defect (fix minimally) or intended behavior (document and close).
  Do not optimize a time window around the trades either way.

method: >
  Pure discovery/citation audit, not new construction. Search research/, runs/, reports/,
  research/registry/ for any existing investigation matching this description before assuming
  none exists (per directive sec3: "do not reconstruct truth from this prompt").

falsification_criterion: n/a -- this is an existence/discovery audit, not a statistical test.
promotion_rule: n/a -- if a defect is found, minimal fix + control reproduction before/after;
  if intended, document and close with no baseline change.
