# GENESIS_H3_XSMOM — RESULT: **NULL (X1·X3 FAIL; held-back 2019+ never loaded)**

Executes `spec.yaml` (committed `dcf0162` before results). Trial `G00012`. Program-printed gates in
`out/gate_table.txt`; 18 pre-computation resolutions in `out/spec_resolutions.txt`.

## Verdict

> **Cross-sectional 12-1 momentum across the certified 21-root futures universe is NOT
> cross-sectional information — it is structure.** Discovery 2009–2018: net **+$349,340** at the
> primary cost model (+$254,555 at the 3-tick stress), positive in both halves and in 6-of-6
> leave-one-sector-out — **but weekly clustered t = 1.43 < 2.0 (X1 FAIL)**, and decisively:
> **the dependence-preserving null earns the same money** (real weekly mean $792 vs null mean
> $922, p95 $3,526; real at the 60.7th percentile — X3 FAIL). Misaligned ranks produce comparable
> P&L, so the earnings come from the vol-scaled long/short mechanics of the decade, not from the
> rank ordering.

- **The 2019-01-01+ held-back window was never loaded** (date filter inside the read; seal_guard
  asserted max 2018-12-31). It remains a pristine confirmation asset for any future family on this
  universe.
- Concentration was healthy (top root 17.6%) — the CARRY failure mode did not recur; the family
  died on information content, not construction.

## Closure scope

CLOSED: *12-1 cross-all-roots rank, top/bottom-6, equal-risk vol-scaled, weekly, 2009–2018, at
this cost model.* NOT closed: other formation horizons (untested and unscheduled — no
neighbour-mining), and nothing about 2019+.

**`LIVE ENABLED = NO` · $0 · DISCOVERY layer.**
