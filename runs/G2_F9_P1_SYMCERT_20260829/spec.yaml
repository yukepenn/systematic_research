run_id: G2_F9_P1_SYMCERT_20260829
campaign: GENESIS_II_FORMAL_WAVE_9
type: SYMMETRIC_CERTIFICATION   # META_ADVERSARY_01 indictment 1 - the champion sits the challengers' exam
question: >
  Does P1/PCT pass the same certification rungs that killed AUCTREV? Every routing decision in
  two campaigns is priced off P1's unexamined status. Either answer reprices the shadow's
  interpretive frame. THIS IS EVIDENCE, NOT A VETO - owner doctrine keeps P1 incumbent
  regardless; results are reported unvarnished.
population: the frozen REPRODUCED 2,131-trade in-window stream (GENESIS_REPRO provenance);
  weekly series 213 ISO weeks; POINTS/dollar P&L as recorded.
valence_precommitment: >
  Mechanism-implied predictions are STATED HERE, before measurement, to prevent the
  asymmetric-valence error the adversary caught:
  P1's mechanism = slow trend-ratchet (holds tens of minutes, re-arms) => R_c prediction:
  +1-bar-delayed entries should degrade net MODERATELY - preregistered consistency band
  [2%, 40%] degradation = CONSISTENT; < 2% = TIMING-IRRELEVANT flag (entry selection may be
  drift-riding; interpret jointly with R_d); > 40% = FASTER-THAN-CLAIMED flag.
rungs_all_coded_and_printed:
  R_a_concentration: top ceil(10%) = 214 trades' share of total net; SAME 40% bar as AUCTREV.
    Also printed (non-gate): top-decile WEEKS share; component shares (the recorded B-MOM ~51%
    flag re-derived from artifacts).
  R_b_loyo: excluding any single calendar year 2022..2026, total net stays > 0 (gate). Era rung
    (pre-2022): NOT-ATTEMPTABLE - printed as a STATUS with the reason (deep substrate params
    frozen on modern data; running it would be a new experiment, not a rung).
  R_c_timing: re-simulate ALL entries one bar (1 min) later on the frozen action set (exits
    unchanged rule: exit logic re-evaluated from the delayed entry, matching EXEC01 E3's
    convention - consistency with EXEC01's -6.4% printed); judged against the PRE-STATED band.
  R_d_mask_null: circular-shift the daily in-market MASK across sessions (>= 300 shifts,
    null_guard sensitivity first): P1's net must exceed the shifted-mask p95 (does WHEN it is
    in the market carry information beyond long drift + duration?). This is the O3-analog the
    incumbent has never faced at mask granularity.
verdict_semantics: each rung PASS / FAIL / NOT-ATTEMPTABLE / CONSISTENT + flags; summary updates
  GENESIS_INCUMBENT_DOSSIER and the shadow's interpretive frame. No automatic demotion; no
  automatic celebration; the multiplicity sentence prints.
prohibitions: no parameter changes, no rescue analysis, no sealed reads, no new economics beyond
  the rungs
outputs: out/gate_table.txt, out/rung_details/, out/ledger_result_pending.json
live_enabled: NO
spend: $0
