run_id: G2_F3_DELEV01_20260829
campaign: GENESIS_II_FORMAL_WAVE_3
type: DISCOVERY_STRATEGY   # card MC-40 - the wave's one new-information shot; short-native
question: >
  After a bear-regime forced-deleveraging day, does continuation persist over the next sessions
  (margin-spiral mechanism, self-limiting)? Four independent evidence lines; the geometry trap
  that killed PDH/PDL is answered with a VOL-MATCHED control.
primary_frozen: >
  Event: RTH close-to-close return in [-5.0%, -2.5%) on a bear-filtered day (close < 200-session
  MA). Position: SHORT 1 contract at next session open; exit at the close 3 sessions later.
  Costs $33/RT (stressed tape). Window 2006 -> 2026-05-31 (pre-burn frozen). Gap-down veto
  (S1-10): if the next session OPENS below the event close by > 1.5%, the entry is VETOED
  (bounce risk) - frozen, not searched.
gates_printed:
  D1: net mean per event > 0 with event-clustered t >= 2.0 (MDE and PER-ERA EVENT COUNTS printed
      BEFORE the table; effective N concentrates in 2008/2020/2022 - stated)
  D2: beats the VOL-MATCHED control - drop-days matched on trailing-RV decile WITHOUT the bear
      filter/band, same policy, count-matched (the PDH/PDL lesson; unfiltered control
      insufficient by design)
  D3: circular-shift null (>= 300 whole-session shifts, ONE shared draw with D2's matching), p95
  diagnostic_non_gate: MFE-path shape (positive early, negative late = spiral-then-reversal is
      the mechanism's sharpest discriminator - reported); per-era table; Hanna-era magnitudes are
      unquotable priors, never benchmarks
verdict: all three -> SURVIVED-DISCOVERY (routes to robustness + independent implementation);
  any fail -> NULL at formulation
prohibitions: no band search ([-5,-2.5) frozen), no horizon search (3 sessions frozen), no MA
  search (200 frozen), no cascade/minutes leg (separate card), no sealed reads
outputs: out/gate_table.txt, out/events.csv, out/ledger_result_pending.json
live_enabled: NO
spend: $0
